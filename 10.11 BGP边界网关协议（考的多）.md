![[Pasted image 20260423223540.png]]
![[Pasted image 20260423224004.png]]
## BGP协议的特点

- BGP是一种实现**AS和AS之间通信的路由协议**
- BGP是**路径矢量路由协议**
- BGP支持**MPLS、VPN**
- BGP是**触发更新、增量更新**
	- 只在启动BGP时交换一次完整信息，后续路由更新报文只通告网络变化信息
- BGP路由有**AS-Path列表**，包含经过的AS
	- **防止环路**
	- 添加 `as-set` 参数
- BGP提供**路由聚合和路由衰减功能**
	- 抑制不稳定路由（一会启用，一会断开），防止路由震荡
- BGP提供了丰富的路由策略
- BGP邻居关系包含EBGP和IBGP
  ![[Pasted image 20260423213120.png|462]]
	- **EBGP：AS间传递路由信息**
		- 物理上直接相连
	- **IBGP：AS内传递外部路由信息**
		- 可以不是直接相连
		- 默认管理距离为200
		- 建议使用loopback地址作为源地址
- BGP使用**TCP的179端口**

## BGP通告原则
- **只发布最优路由**
	- 从IGP内部网关协议（RIP、OSPF）中得到![[Pasted image 20260423215319.png]]
- **从EBGP对等体获取的路由，会发布给所有对等体（EBGP、IBGP）**
  ![[Pasted image 20260423215357.png|414]]
- **IBGP水平分割：从IBGP对等体获取的路由，不会发送给其他IBGP对等体**
	- 防止IBGP邻居互相通告导致产生环路
	- 规避路由黑洞
	- IBGP可以把学习的路由发送给EBGP

## BGP反射器

- 角色：**RR路由反射器**和Client客户端
- RR会将学习的路由反射出去，从而使IBGP路由在AS内传播时无需建立IBGP全互连
	- **打破IBGP水平分割规则，可能导致环路**
- 反射规则：**非客户端不传非客户端**
	- 从**非客户端**收到的IBGP路由，反射器只会传递给**所有客户端**
	- 从**客户端**收到的IBGP路由，反射器会传递给**所有客户端和非客户端**
	- 从**EBGP对等体**收到的路由，反射器会传递给**所有客户端和非客户端**
![[Pasted image 20260423232801.png|387]]

### 避免反射导致的环路（填空题）

为BGP路由添加两个路径属性来避免出现环路
- `Originator_ID`：表示发送该条路由的BGP路由器Router ID
	- 当BGP路由器收到路由条目时，比较 `Originator_ID` 与自身Router ID，若相同则表示该条路由是自己发出的，直接忽略该条路由的更新
	  ![[Pasted image 20260423233157.png|383]]
- `Cluster_List` 路径反射簇：当一跳路由被反射器反射后，该反射器的Cluster_ID会被添加至路由的 `Cluster_List` 中。
	- `reflector cluster-id 集群ID` 配置路由反射器的集群ID， **默认情况下集群ID为Router ID**
	- 当RR收到一条携带  `Cluster_List` 的BGP路由时，如果  `Cluster_List` 包含自身所属的 Cluster_ID，则忽略该条路由的更新
	  ![[Pasted image 20260423233516.png]]

## BGP配置

- 启动BGP进程并指定路由器的Router ID
```shell
bgp AS_ID # 启动BGP进程，并指定AS号
	router-id IP地址 # 配置BGP路由器的Router ID
```
- 配置BGP对等体
```shell
peer IP地址 as-number AS_ID # 创建BGP对等体，并指定对等体地址和AS号
```
- 配置建立对等体使用的源端口、EBGP对等体最大跳数
	- 默认情况下EBGP连接允许最大跳数为1，即只能在屋里直连链路上建立EBGP连接
	- 要使用Loopback端口建立连接，需要设置最大跳数
	- IBGP源端口：loopback
	- EBGP源端口：直接相连的端口
```shell
peer IP地址 connect-interface 端口号 [源端口IP地址] # 指定发送BGP报文的源端口、源地址
peer IP地址 ebgp-max-hop 跳数 # 指定建立EBGP连接允许的最大跳数
```
- 配置当前路由器为反射器，并指定其客户端 `peer IP地址 reflect-client` 
	- 默认情况下不配置RR和客户端
-  `reflector cluster-id 集群ID` 配置路由反射器的集群ID
	- **默认情况下集群ID为Router ID**
- 查看BGP路由表 `display bgp routing-table`
- 配置发送给IBGP邻居的路由的下一跳为自身的IP地址 `peer IP地址 next-hop-local`
- 开启BGP环路检测功能 `route loop-detect bgp enable`
- 路由聚合
```shell
bgp AS_ID # 启动BGP进程，并指定AS号
	summary automatic # 配置自动路由聚合
	aggregate IP地址 子网掩码  # 配置手动路由聚合
```

### 配置对等体
![[Pasted image 20260423220353.png]]
![[Pasted image 20260423220521.png]]
![[Pasted image 20260423220709.png]]

![[Pasted image 20260423222725.png]]
![[Pasted image 20260423222906.png]]
![[Pasted image 20260423222958.png]]
![[Pasted image 20260423223222.png]]
- SwitchB只转发了SwitchA的路由，并没有更改Next-Hop，所以下一跳仍然是192.168.1.2，后续的路由器不知道怎么去这一跳，解决方法
	- 通过 `import-route`更新SwitchB的路由表，从而进一步发送Update给邻居路由
	- 配置发送给IBGP邻居的路由的下一跳为自身的IP地址 `peer IP地址 next-hop-local`

## BGP路由属性

![[Pasted image 20260423220924.png]]

### Origin属性

- BGP自身不会生成路由，BGP将IGP路由表的路由注入到BGP路由表中，并通过Update报文传递给EBGP或IBGP，引入方式包括
	- `network IP地址 子网掩码`
	- `import-route 协议名`
- Origin属性的标记
	- `i`：通过 `network`命令注入到BGP的路由，该路由属性为`IGP`
	- `e`：通过EBGP学习到的路由，该路由属性为`EGP`
	- `?`：通过 `import-route`引入的路由，该路由属性为`Incomplete`
	- 优先级 `IGP` > `EGP` > `Incomplete`
![[Pasted image 20260423221020.png]]
![[Pasted image 20260423221221.png]]
![[Pasted image 20260423221447.png]]

### Next_Hop属性

- 将路由发给EBGP对等体时：`Next_Hop`设置为自己的**TCP连接源地址**
- 将路由发给IBGP对等体时：`Next_Hop` 不变
	- 使用 `peer 对端IP地址 next-hop-local` 将下一跳属性设置为自己的**TCP连接源地址**
![[Pasted image 20260423221608.png]]
![[Pasted image 20260423221718.png]]

### AS_Path属性

- 需要添加 `as-set` 参数
![[Pasted image 20260423221910.png]]![[Pasted image 20260423221948.png]]

## BGP路由聚合

- BGP路由聚合在IPv4中支持**自动聚合**和**手动聚合**，在IPv6中仅支持**手动聚合**
- 在BGP视图下配置自动聚合 `summary automatic`
- 在BGP视图下配置手动聚合 `aggregate IP地址 子网掩码`
![[Pasted image 20260423222121.png]]

## BGP报文类型和邻居状态

![[Pasted image 20260424001014.png|681]]
![[Pasted image 20260423222234.png]]
BGP状态变化
- Idle状态：等待TCP连接
- Connect状态：正在进行TCP连接
- Active状态：TCP连接简历成功
- **OpenSent状态**：开始发送 **Open报文**
- **OpenConfirm状态**：收到 **Open报文**，并发送 **Keepalive报文**
- Established状态：双方均收到 **Keepalive报文**，开始使用 **Update报文** 通告路由信息


![[Pasted image 20260423223815.png]]