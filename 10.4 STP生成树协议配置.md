
- **主要考基本原理简答题**
- 了解代码

## STP配置

在交换机进行设置
- **打开或关闭STP功能** `STP {enable | disable}`
- **运行生成树协议STP或RSTP模式** `stp mode {stp | rstp}`
- **设置为根桥或备份根桥** `stp root {primary | secondary}`
- 设置交换机优先级 `stp priority 优先级`
	- 优先级越低，交换机成为根桥的概率越高

在交换机的端口进行设置
- 设置端口开销 `stp cost 开销值`
- 设置端口优先级 `stp port priority 优先级`
- **设置端口为边缘端口** `stp edged-port enable`
	- 边缘端口直接与终端设备直连，不接收处理BPDU报文
	- 加速端口进入转发状态
	- 手动配置边缘端口的优先级，优先级高于自动监测机制
- 启用端口BPDU报文过滤功能 `stp bpdu-filter enable`
	- 使边缘端口不处理、不发送BPDU报文

![[Pasted image 20260415202113.png]]
## MSTP配置

- 进入MST域视图 `stp region-configuration`
- 配置MST域的域名 `region-name 域名`
	- 默认情况下，域名为设备MAC地址
- 配置vlan映射表 `instance INSTANCE_ID vlan VLAN_LIST`
	- 默认情况下，所有VLAN都映射到MSTI0上
- 激活MST域的配置（必选） `active region-configuration`
- 配置MSTP工作模式（必选） `stp mode {stp | rstp | mstp}`
	- 默认工作模式为MSTP模式
- 配置当前设备为指定MSTI的根桥 `stp [instance INSTANCE_ID] root primary`
- 配置设备优先级 `stp [instance INSTANCE_ID] priority 优先级`
	- 默认情况下为 `32768`
- 配置端口路径开销 `stp [instance INSTANCE_ID] cost 开销值`
	- 默认情况下，自动按照响应标准计算路径开销
- 配置端口优先级 `stp [instance INSTANCE_ID] port priority 优先级`
	- 默认情况下为 `128`