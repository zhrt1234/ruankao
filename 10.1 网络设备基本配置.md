
![[Pasted image 20260413153412.png]]
网络设备配置方式
- console：通过console线缆连接**PC的串行端口**和**交换机的console口**
	- `user-interface console 0`
- telnet、stelnet、ssh方式
	- `user-interface vty 1~4`
- web方式

### vty配置
```shell
telnet server enable # 开启telnet服务
user-interface vty 0 4 # 进入VTY用户视图界面
	protocol inbound telnet # 配置VTY支持telnet协议
	authentication-mode {aaa | password | none} # 设置认证模式为aaa认证、口令认证
	quit
aaa # 进入aaa视图
	local-user 用户名 password irreversible-cipher 密码 # 配置用户名和密码
	local-user 用户名 privilege level {0~15} # 配置用户权限等级，3为管理员级别
	local-user 用户名 service-type telnet # 配置本地用户接入类型为telnet
```

### console配置
```shell
user-interface console 0 # 进入console用户视图界面
	authentication-mode {aaa | password | none} # 配置认证方式
	set authentication password cipher 密码 # 设置密码
```


![[Pasted image 20260413154059.png]]
网络配置视图总结

![[Pasted image 20260413162015.png]]
用户权限级别
- 3-15级都是管理级

常见display命令
- `display current-configuration`：系统当前配置信息
- `display saved-configuration`：系统已保存的配置信息
- `display interface`：查看接口信息
- `display interface brief`：查看接口简要信息
- `display vlan verbose`：查看vlan详细信息
- `display stp`：查看STP的状态和统计简要信息
- `display arp`：查看ARP映射表
- `display ip pool`：查看设备上已经配置的IP地址池信息
- `display ip routing-table`：查看路由表
- `display ospf lsdb`：查看OSPF链路状态数据库信息
- `display ospf peer`：查看OSP邻居
- `display bgp peer`：查看BGP对等体
- `display bgp routing-table`：查看BGP路由表

