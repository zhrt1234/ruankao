
从其他路由协议引入路由，使用`import-route`命令来配置引入的路由协议、进程
- `import-route {ospf | isis | rip} [进程号] [cost 开销] [route-policy 路由策略名] [tag 标记] [type 类型]`
- `import-route {direct | static | bgp | permit-ibgp} [cost 开销] [route-policy 路由策略名] [tag 标记] [type 类型]`
	- route-policy 路由策略，例如屏蔽某些端口
	- `type`
		- `E1` 考虑外部开销 + 内部开销
		- `E2` 默认，仅考虑外部开销
- 删除路由引入 
	- `undo import-route {ospf | isis | rip} [进程号]`
	- `undo import-route {direct | static | bgp | permit-ibgp}`
- 生成RIP默认路由并转发给周边路由器 `default-route originate`
- 生成OSPF默认路由并转发给周边路由器`default-route-advertise`

![[Pasted image 20260423202755.png]]
![[Pasted image 20260423203023.png]]

设置缺省路由的度量值为2
```shell
<HUAWEI> system-view
[HUAWEI] rip 100
[HUAWEI-rip-100] default-route originate cost 2
```