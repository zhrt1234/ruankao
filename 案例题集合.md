
- 负载分担策略
	- 基于目的地址的负载分担
	- 基于源地址的负载分担
	- 基于带宽算法负载分担
	- 基于应用识别负载分担
- 服务器负载均衡算法
	- 简单轮询算法
	- 加权轮询算法
	- 最小连接算法
	- 加权最小连接算法
	- 源IP hash算法
	- 加权源IP hash算法
- **堆叠、集群**
- **AC和AP**
	- AC挂载方式
	- 隧道、频谱导航
	- 配置步骤
	- WiFi信号对可靠性的影响因素
- **RAID**
- **集中式存储、分布式存储**
- **网络设计步骤、六大子系统、施工规范**
- **VRRP**
- **网络安全**
	- 入侵方式和防范措施
	- 病毒
	- 防火墙类型、安全区域、出入流量方向
	- 安全技术：加密、报文摘要
- **网络设备基础配置**
	- console视图
	- vty视图
- **vty和console视图**
- **端口隔离**
	- `port-isolate enable group`、`port-isolate mode l2`
	- `arp-proxy inner-sub-vlan-proxy enable`
	- `mac-address static`、`mac-address blackhole`
- **端口安全**
	- `mac-address learning disable`、`mac-limit maximum`、`mac-limit action {discard|forward}`
	- `port-security enable`、`port-security mac-address sticky` 、`port-security protection-action {restrict|protect|shutdown}`、`port-security max-mac-num`
- **MAC地址漂移**
	- `mac-address flapping detection`、`mac-address flapping action error-down`、`mac-address flapping aging-time`
	- `mac-learning priority`
- **VLAN**
	- 基于端口划分vlan
	- 基于MAC划分vlan：`mac-vlan mac-address`
	- 基于子网划分vlan：`ip-subnet-vlan ip`
	- vlan聚合：`aggregate-vlan`、`access-vlan VLAN_ID`、`arp-proxy inter-sub-vlan-proxy enable`
	- VLAN间通信（单臂路由）：逻辑子接口、`dot1q termination vid`、`arp broadcast enable`
	- 基本QinQ：`qinq protocol`、`port link-type dot1q-tunnel`
	- 灵活QinQ：`qinq vlan-traslation enable`、`port vlan-stacking 内vlan vlan-stack 外vlan`
	- vlan mapping：`port vlan-mapping vlan 外vlan inner-vlan 内vlan map-single-vlan 其它vlan`
- **DHCP**
	- 系统视图执行 `dhcp enable`、接口视图执行 `dhcp select {global|interface|relay}`
	- 全局模式、IP地址池：`ip pool POOL_NAME`、`display ip pool [name POOL_NAME]`、`gateway-list`、`dns-list`、`network IP mask MASK`、`excluded-ip-address`、`leasy day DAY hour HOUR minute MIN`、`domain-name`、`static-bind ip-address IP mac-address MAC`
	- 端口模式：在全局模式的指令前加上 `dhcp server`
	- DHCP中继：`dhcp relay server-ip IP地址`
	- DHCP Snooping：`dhcp snooping enable`、`dhcp snooping trusted`
	- 配置流程、全局模式与端口模式区别
- **路由**
	- `display ip routing-table`
	- 路由优先级
	- 最长网络前缀匹配原则
- **静态路由**
	- `ip route-static`
	- 等价静态路由：目的相同、下一跳不同、负载均衡
	- 浮动静态路由：目的相同、下一跳不同、优先级不同、链路冗余
	- 黑洞路由：`ip route-static IP MASK null0`
- **RIP**
	- UDP 520端口
	- 特点、工作原理、RIPv1与RIPv2区别
	- `network IP`不加子网掩码
	- `rip 进程号`、`version {1|2}`、`display rip 进程号 route`、`undo summary`
- **BFD**
	- 链路故障感知，路由快速收敛
	- `bfd`、`bfd all-interfaces enable`
	- `bfd BFD_ID bind peer-ip 对方IP interface 本端口 source-ip 本IP [one-arm-echo]`
	- `rip bfd enable`、`rip bfd block`、`rip bfd static`
- **OSPF**
	- IP 协议号89
	- 工作原理、route-id
	- OSPF网络类型：`ospf network-type {broadcast|p2p}`
	- DR/BDR：`ospf dr-priority`、选举规则、作用范围为一个网段（广播域）
	- OSPF状态：`down`、`init`、`2-way`、`full`
	- OSPF分区管理：ABR/ASBR、区域类型、LSA类型
	- OSPF配置：`ospf 进程号 route-id ID`、`network IP 反掩码`、`display ospf lsdb`、`display ospf peer brief`
	- OSPF区域认证：`authentication-mode {simple|hmac-md5|hmac-sha256} KEY_ID {plain|cipher} PASSWORD`、在接口上配置需要在指令前加`ospf`
	- OSPF静默端口：禁止端口接收或发送OSPF报文 `silent-interface {all|端口号}`
	- OSPF虚连接：`vlink-peer ROUTER_ID`
	- OSPF路由汇总：`abr-summary IP MASK`、`asbr-summary IP MASK`
	- OSPF与RIP区别
- **路由引入**
	- `import-route {direct|static|ospf|rip} [进程号] [cost] [route-policy 路由策略] [type {E1|E2}]`
	- `import-route bgp [permit-ibgp]` 添加`permit-ibgp`表示允许引入IBGP路由
- **BGP**
	- TCP 179端口
	- 通告原则：只发布最优路由、从EBGP获取的路由通告给所有BGP对等体、IBGP水平分割
	- BGP反射器
		- 反射规则（非客户端不传给非客户端）
		- 打破IBGP水平分割原则，可能导致路由环路
		- 避免环路：`Originator_ID`、`Clust_List`
		- `reflector cluster-id {路由ID|集群ID}`、`peer 对方IP {reflect-client|next-hop-local}`
	- BGP配置：`display bgp routing-table`、`bgp AS_ID`、`peer {对方IP|Loopback IP} as-number 对方AS`、`peer 对方IP connect-interface 端口号 IP地址`、`peer 对方IP ebgp-max-hop`、`route loop-detect bgp enable`
	- Origin属性：`i` 通过network引入， `e`通过EBGP学习， `?` 通过import-route引入
	- Next-Hop属性：发给EBGP对等体next-hop设置为自己，发给IBGP对等体next-hop不变（`next-hop-local`）
	- AS_Path属性：避免环路，需要添加`as-set`
	- BGP路由聚合：`aggregate IP MASK`、`summary automatic`
	- BGP状态变化
- **ip-prefix**
	- `ip ip-prefix 前缀名 [index INDEX_ID] {permit|deny} IP 前缀长度`
- **ACL**
	- 匹配规则：从前往后
	- 流量方向：inbound（进入端口）、outbound（从端口流出）
	- 基本ACL：`acl [number] {2000~2999} [match-order {auto|config}]`、`rule [RULE_ID] {permit|deny} source {IP 反掩码|any}`
	- 高级ACL：`acl [number] {3000~3999}`、`rule [RULE_ID] {permit|deny} [tcp|udp|ip|icmp] [source IP 反掩码] [destination IP 反掩码] [比较符 端口号]`
	- 命名ACL：`acl name 名称 {basic|advance|编号}`
	- 基于时间的ACL：`time-range 时间段名 {TIME1 to TIME2|from TIME1 to TIME2}`、`rule {permit|deny} time-range 时间段名`
	- ACL部署位置：部署在三层设备、高级ACL靠近源、基本ACL靠近目的
- NAT
- IPSec VPN（框架、原理）
- 策略路由（配置步骤）
- 路由环路
- 路由策略
- 防火墙
