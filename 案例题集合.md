
- 负载均衡策略
	- 基于源IP地址的负载均衡
	- 基于目的IP地址的负载均衡
	- 基于链路质量的负载均衡
	- 基于链路带宽的负载均衡
	- 基于链路优先级的负载均衡
	- 基于应用的负载均衡
- 服务器负载均衡算法
	- 简单轮询算法
	- 加权轮询算法
	- 最小连接算法
	- 加权最小连接算法
	- 源IP hash算法
	- 加权源IP hash算法
- **堆叠、集群**
- **AC和AP**
	- AC挂载方式
	- 隧道、频谱导航
	- 配置步骤
	- WiFi信号对可靠性的影响因素
- **RAID**
- **集中式存储、分布式存储**
- **网络设计步骤、六大子系统、施工规范**
	- 网络带宽需求分析：业务流量特征、用户规模与并发量、未来扩展需求
	- 无线接入需求分析：信号强度与覆盖范围、用户终端类型、接入密度、安全认证需求
- **网络安全**
	- 入侵方式和防范措施
	- 病毒
	- 防火墙类型、安全区域、出入流量方向
	- 安全技术：加密、报文摘要
	- 钓鱼攻击
		- 加强员工网络安全意识培训，摇成良好的网络使用习惯
		- 定期开展钓鱼攻击演习
		- 加强网络安全建设，使用多因素身份认证提高账户安全性
		- 实时更新系统和软件
	- DNS欺骗攻击
		- 配置更可信的DNS服务器
		- 使用内部DNS服务器
		- 检查本地DNS设置
		- 清空DNS缓存
		- 使用加密DNS
		- 部署防火墙、IPS
- **vty和console视图**
- **端口隔离**
	- `port-isolate enable group GROUP_ID`、`port-isolate mode {l2|all}`
	- `arp-proxy inner-sub-vlan-proxy enable`
	- `mac-address static`、`mac-address blackhole`
- **镜像端口**
	- `observe-port 观察端口ID interface 端口号`
	- `port-mirroring to observe-port 观察端口ID {inbound|outbound}`
- **端口安全**
	- `mac-address learning disable`、`mac-limit maximum`、`mac-limit action {discard|forward}`
	- `port-security enable`、`port-security mac-address sticky` 、`port-security protection-action {protect|restrict|shutdown}`、`port-security max-mac-num`
- **MAC地址漂移**
	- `mac-address flapping detection`、`mac-address flapping action error-down`、`mac-address flapping aging-time`
	- `mac-learning priority`
- **VLAN**
	- 基于端口划分vlan
	- 基于MAC划分vlan：`mac-vlan mac-address MAC地址`
	- 基于子网划分vlan：`ip-subnet-vlan ip IP地址 子网掩码`
	- vlan聚合：`aggregate-vlan`、`access-vlan VLAN_ID`、`arp-proxy inter-sub-vlan-proxy enable`
	- VLAN间通信（单臂路由）：逻辑子接口`interface g0/0/1.1`、`dot1q termination vid VLAN_ID`、`arp broadcast enable`
	- 基本QinQ：`port link-type dot1q-tunnel`、`qinq protocol 外层VLAN_ID`
	- 灵活QinQ：`qinq vlan-traslation enable`、`port vlan-stacking vlan 内VLAN_ID stack-vlan 外VLAN_ID`
	- vlan mapping：`port vlan-mapping vlan 外VLAN_ID inner-vlan 内VLAN_ID map-single-vlan 其它VLAN_ID`
- **DHCP**
	- 系统视图执行 `dhcp enable`、接口视图执行 `dhcp select {global|interface|relay}`
	- 全局模式、IP地址池：`ip pool POOL_NAME`、`display ip pool [name POOL_NAME]`、`gateway-list`、`dns-list`、`network IP mask MASK`、`excluded-ip-address`、`leasy day DAY hour HOUR minute MIN`、`domain-name`、`static-bind ip-address IP mac-address MAC`
	- 端口模式：在全局模式的指令前加上 `dhcp server`
	- DHCP中继：`dhcp relay server-ip IP地址`
	- DHCP Snooping：`dhcp snooping enable`、`dhcp snooping trusted`
	- 配置流程、全局模式与端口模式区别
- **路由**
	- `display ip routing-table`
	- 路由优先级
	- 最长网络前缀匹配原则
- **静态路由**
	- `ip route-static`
	- 等价静态路由：目的相同、下一跳不同、负载均衡
	- 浮动静态路由：目的相同、下一跳不同、优先级不同、链路冗余
	- 黑洞路由：`ip route-static IP MASK null0`
- **RIP**
	- UDP 520端口
	- 特点、工作原理、RIPv1与RIPv2区别
	- `network IP`不加子网掩码
	- `rip 进程号`、`version {1|2}`、`display rip 进程号 route`、`undo summary`、`default-route originate`
- **BFD**
	- 链路故障感知，路由快速收敛
	- `bfd`、`bfd all-interfaces enable`
	- `bfd BFD_ID bind peer-ip 对方IP interface 本端口 source-ip 本IP [one-arm-echo]`
	- `rip bfd enable`、`rip bfd block`、`rip bfd static`
- **OSPF**
	- IP 协议号89
	- 工作原理、route-id
	- OSPF网络类型：`ospf network-type {broadcast|p2p}`
	- DR/BDR：`ospf dr-priority`、选举规则、作用范围为一个网段（广播域）
	- OSPF状态：`down`、`init`、`2-way`、`full`
	- OSPF分区管理：ABR/ASBR、区域类型、LSA类型
	- OSPF配置：`ospf 进程号 route-id ID`、`network IP 反掩码`、`display ospf lsdb`、`display ospf peer brief`
	- OSPF认证方式
		- 根据报文类型分为：区域认证、接口认证
		- 根据报文的认证方式分为：简单认证、MD5认证、HMAC-SHA256认证
		- OSPF区域认证：`authentication-mode {simple|hmac-md5|hmac-sha256} KEY_ID {plain|cipher} PASSWORD`
		- OSPF接口认证：在指令前加`ospf`
	- OSPF静默端口：禁止端口接收或发送OSPF报文 `silent-interface {all|端口号}`
	- OSPF虚连接：`vlink-peer ROUTER_ID`
	- OSPF路由汇总：`abr-summary IP MASK`、`asbr-summary IP MASK`
	- OSPF环路检测：`route loop-detect ospf enable`
	- OSPF与RIP区别
- **路由引入**
	- `import-route {direct|static|ospf|rip} [进程号] [cost] [route-policy 路由策略] [type {E1|E2}]`
	- `import-route bgp [permit-ibgp]` 添加`permit-ibgp`表示允许引入IBGP路由
- **BGP**
	- TCP 179端口
	- 通告原则：只发布最优路由、从EBGP获取的路由通告给所有BGP对等体、IBGP水平分割
	- BGP反射器
		- 反射规则（非客户端不传给非客户端）
		- 打破IBGP水平分割原则，可能导致路由环路
		- 避免环路：`Originator_ID`、`Clust_List`
		- `reflector cluster-id {路由ID|集群ID}`、`peer 对方IP {reflect-client|next-hop-local}`
	- BGP配置：`display bgp routing-table`、`bgp AS_ID`、`peer {对方IP|Loopback IP} as-number 对方AS`、`peer 对方IP connect-interface 端口号 IP地址`、`peer 对方IP ebgp-max-hop`、`route loop-detect bgp enable`
	- Origin属性：`i` 通过network引入， `e`通过EBGP学习， `?` 通过import-route引入
	- Next-Hop属性：发给EBGP对等体next-hop设置为自己，发给IBGP对等体next-hop不变（`next-hop-local`）
	- AS_Path属性：避免环路，需要添加`as-set`
	- BGP路由聚合：`aggregate IP MASK`、`summary automatic`
	- BGP状态变化
- **VRRP**
	- 优先级越大越优先，默认优先级100
	- 抢占模式：确保优先级最高的路由器成为Master
	- 创建多个VRRP组：充分利用链路资源、实现负载均衡
	- 心跳线：用于传递VRRP协议报文
	- VRRP配置：`vrrp vrid VRID virtual-ip IP`、`vrrp vrid VRID priority 优先级`、`vrrp vrid VRID preempt-mode timer delay 时间`、`vrrp vrid VRID track interface 端口号 reduced 优先级`、`display vrrp [verbose|statistics]`
- **ip-prefix**
	- `ip ip-prefix 前缀名 [index INDEX_ID] {permit|deny} IP 前缀长度`
- **ACL**
	- 匹配规则：从前往后
	- 流量方向：inbound（进入端口）、outbound（从端口流出）
	- 基本ACL：`acl [number] {2000~2999} [match-order {auto|config}]`、`rule [RULE_ID] {permit|deny} source {IP 反掩码|any}`
	- 高级ACL：`acl [number] {3000~3999}`、`rule [RULE_ID] {permit|deny} [tcp|udp|ip|icmp] [source IP 反掩码] [destination IP 反掩码] [比较符 端口号]`
	- 命名ACL：`acl name 名称 {basic|advance|编号}`
	- 基于时间的ACL：`time-range 时间段名 {TIME1 to TIME2|from TIME1 to TIME2}`、`rule {permit|deny} time-range 时间段名`
	- ACL部署位置：部署在三层设备、高级ACL靠近源、基本ACL靠近目的
	- 不需要acl前缀：nat、filter-policy
- **策略路由policy-based-route**（配置步骤）
	- 配置ACL访问控制列表
	- 配置策略路由，通过 `if-match`语句过滤数据流，通过 `apply` 语句对数据流实施操 `policy-based-route 策略名 {permit|deny} node 节点ID`
	- 在端口上应用策略路由 `ip policy-based-route 策略名`
- **路由策略route-policy**
	- 配置代码：`route-policy 路由策略名 {permit|deny} node 节点ID`
	- if-match子句：`if-match {acl ACL编号|ip-prefix IP前缀名|interface 端口号}`
	- apply子句：`apply {ip-address next-hop 下一跳|output-interface 端口号|preference 优先级|cost-type {type-1|type-2}|cost 路由开销}`
	- 使用route-policy进行路由控制：`import-route {direct|ospf} route-policy 路由策略名`
	- 路由环路问题：使用route-policy修改优先级、过滤路由
- **路由过滤filter-policy**：用于OSPF、BGP等协议对路由进行过滤
	- `filter-policy {ACL编号|ip-prefix IP前缀名} import
	- `filter-policy {ACL编号|ip-prefix IP前缀名} export {direct|static}`
- **NAT**
	- 端口多路复用：同一个公网IP，使用端口号区分私网 `mode pat`
	- NAT地址池：`nat address-group 地址池名 [起始IP地址 终止IP地址]`、`section 起始IP地址 终止IP地址`、`route enable`、`mode pat`
	- 启用NAT：`nat outbound ACL编号 address-group 地址池名 [no-pat]`
	- 启用EasyIP，外网地址池为出接口的IP地址： `nat outbound ACL编号`
	- NAT Server：让外网用户可以访问内网 `nat server [protocol 协议名] global {外网地址|current-interface} [端口号] inside 内网地址 [端口号]`、`undo nat server`
	- 防火墙上配置NAT
		- 配置端口IP：`ip address`
		- 配置出口路由、回程路由：`ip route-static`
		- 配置防火墙安全区域（trust、DMZ、untrust、local）：`firewall zone {trust|untrust|dmz|local}`、`add interface 端口号`
		- 配置防火墙的 `security-policy`，允许域间访问：`security-policy`、`rule name 安全策略名`、`source-zone {trust|untrust}`、`destination-zone {trust|untrust}`、`source-address IP地址 掩码长度`、`action {permit|deny}`
		- 创建NAT地址池
		- 配置 `nat-policy` NAT策略：`nat-policy`、`rule name NAT策略名`、`source-zone`、`destination-zone`、`source-address`、`action source-nat address-group NAT地址池名`
	- 黑洞路由：为防止因NAT引起的路由环路，需要在防火墙上配置黑洞路由 `ip route-static NAT转化后的地址 32 NULL0`
	- 防火墙的源进源出功能：`redirect-reverse next-hop 网关IP地址`、`gateway 网关IP地址`
- IPSec VPN（框架、原理）
	- 配置ACL定义需要保护的数据流
	- 配置 `ipsec proposal` 安全提议：`ipsec proposal 安全提议名`、`encapsulation-mode tunnel`、`transform {esp|ah}`、`esp encryption-algorithm aes-256`、`esp authentication-algorithm sha2-256`
	- 配置 `ike peer` IKE对等体：`ike peer 对等体名`、`pre-shared-key {simple|cipher} 密码`、`remote-address IP地址`、`ike-proposal IKE提议序号`
	- 配置 `ipsec policy` IPSec安全策略： `ipsec policy 策略名 策略序号 isakmp`、`security acl ACL编号`、`proposal 安全提议名`、`ike-peer IKE对等体名`、
	- 在接口上应用IPSec安全策略：`ipsec policy 策略名`
- MQC流分类、流行为、流策略
- MPLS