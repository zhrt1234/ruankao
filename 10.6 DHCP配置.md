**考察较少**

**华为配置关键命令**：
- 全局模式：`dhcp select global` + `ip pool`
- 接口模式：`dhcp select interface`
- 中继模式：`dhcp select relay` + `dhcp relay server-ip`

## 配置全局模式

- 在**路由器或三层交换机上创建全局地址池**，通过该地址池为同一子网或多个子网的客户端分配 `IP` 地址。
- **特点（要考）**
	- 集中化管理地址池
	- 地址池中的地址可以分配给设备所有接口下的客户端
	- 当DHCP服务器与客户端不在同一网段时，需要部署DHCP中继
![[Pasted image 20260419155353.png|327]]

```shell
dhcp enable # 在服务器上开启DHCP功能
ip pool POOL_NAME # 创建IP地址池
	gateway-list 网关IP # 指定网关，可配置多个网关
	network 网络号 mask 子网掩码 # 创建网段
	dns-list 8.8.8.8 # 指定DNS服务器
	static-bind ip-address IP地址 mac-address MAC地址 # 分配固定IP
	excluded-ip-address IP_ADDR1 IP_ADDR2 # 不参与自动分配的IP地址
	lease day 7 hour 0 minute 0 # 设置租约时间
	domain-name 域名 # 设置DHCP服务器域名

interface 端口号 # 进入物理端口
	ip address IP地址 子网掩码 # 设置端口的IP地址
	dhcp select global # 开启DHCP全局模式
```

- 根据pool名称显示地址池信息 `display ip pool name 名称`
- 显示所有的地址池信息 `display ip pool`
## 基于端口的配置方式

- 直接在接口（如 `VLANIF` 或物理接口）下启用 `DHCP` 服务，并基于接口所在子网自动生成地址池。
- **特点（要考）**
	- DHCP服务器与客户端处于同一网段
	- 配置简单，无需单独配置地址池
	- 适用于单一子网且参数简单的场景
![[Pasted image 20260419155425.png|273]]

先配置物理端口的IP网关
```shell
dhcp enable # 开启DHCP服务
interface 端口号 # 进入物理端口
	ip address IP地址 子网掩码 # 配置端口IP地址
	dhcp select interface # 开启DHCP端口模式
	dhcp server gateway-list 网关IP # 指定网关，可配置多个网关
	dhcp server dns-list 8.8.8.8 # 设置DNS服务器
	dhcp server static-bind ip-address IP地址 mac-address MAC地址 # 分配固定IP
	dhcp server excluded-ip-address IP_ADDR1 IP_ADDR2 # 不参与自动分配的IP地址
	dhcp server lease day 7 hour 0 minute 0 # 设置租约时间
	
```

## DHCP中继配置

- 通过 **DHCP中继代理** 将客户端的广播请求转发到其他子网的 `DHCP` 服务器，实现跨子网地址分配。
- 特点：需指定远端 `DHCP` 服务器地址，支持复杂网络架构。

![[Pasted image 20260419160450.png|360]]

路由器、核心交换机上配置
```shell
dhcp enable # 开启DHCP
interface 端口号 # 进入端口
	ip address IP地址 子网掩码 # 配置端口IP地址
	dhcp select relay # 开启中继模式
	dhcp relay server-ip 服务器IP # 指定DHCP服务器IP
```

## DHCP snooping

DHCP Snooping 是一种网络安全功能，用于防止非法的 DHCP 服务器分配 IP 地址。
![[Pasted image 20260419161939.png|422]]

交换机全局启用DHCP Snooping
```shell
dhcp enable
dhcp snooping enable
```
配置信任端口
```shell
interface 端口号
	dhcp snooping trusted # 设置当前端口为信任端口
```
用户侧端口启用DHCP Snooping
```shell
interface 端口号
	dhcp snooping enable
```

![[Pasted image 20260419162857.png]]
案例题
![[Pasted image 20260419163913.png]]![[Pasted image 20260419163919.png]]