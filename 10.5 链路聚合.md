- **考察作用、适用场景，代码考察较少**
- **掌握优先级、mode指定**

## 链路聚合配置方式

- 手工方式
- 静态LACP
- 动态LACP
### 手工方式

![[Pasted image 20260418225416.png]]
- **创建eth-trunk接口** `interface eth-trunk 接口ID`
- **将端口加入eth-trunk** `trunkport 端口ID1 to 端口ID2`
- **配置eth-trunk负载分担方式** `load-balance {src-dst-mac | src-dst-ip | dsp-ip | dst-mac | src-ip |src-mac}`
	- 默认情况下为 `src-dst-ip`
- 显示eth-trunk状态 `display eth-trunk 接口ID`
- 配置eth-trunk，类似于配置一条trunk端口
```
interface eth-trunk 1  # 进入eth-trunk
port link-type trunk   # 设置link-type
port trunk allow-pass vlan 10 20  # 允许vlan10和vlan20通过
quit
```
  
![[Pasted image 20260418225608.png]]![[Pasted image 20260418225634.png]]![[Pasted image 20260418225735.png]]
![[Pasted image 20260418231737.png]]
- **手动配置的working mode为normal**

### 静态LACP配置

eth-trunk设置，需要进入eth-trunk视图
- 设置静态LACP模式 `mode lacp-static`
- 设置eth-trunk最大活跃端口数 `max active-linknumber 数量`

端口设置，需要进入端口视图
- 将端口加入eth-trunk（**也可以使用trunkport**） `eth-trunk ETH-TRUNK_ID`
- 设置端口的lacp优先级，数值越小优先级越高，优先成为活跃端口 `lacp priority 优先级`

![[Pasted image 20260418230846.png]]
![[Pasted image 20260418230914.png]]
![[Pasted image 20260418231255.png]]
![[Pasted image 20260418231710.png]]
![[Pasted image 20260418231751.png]]

![[Pasted image 20260418232626.png]]
错题
![[Pasted image 20260418233321.png]]
原因：在core与防火墙通信时，防火墙发给core的数据MAC地址一直是core的MAC地址（因为防火墙相当于路由设备），因此负载分担算法一直使用同一条链路。
解决方法：将负载分担算法改为src-dst-ip，即基于IP地址的负载分担