
## VRRP定义

![[Pasted image 20260425133809.png]]


### VRRP报文

- VRRP报文封装在**IP报文**
- VRRP协议报文用来**将Master设备的优先级和状态**通告给同一VRRP组的所有Backup设备

### VRRP相关概念

- VRID：用于标识同一个VRRP组
- 优先级：用于选举Master设备
	- **值越大越优先**（0~255）
	- **默认优先级100**
- IP地址拥有者：如果**VRRP虚拟IP地址与某一个设备的接口IP地址相同**，则该设备成为Master
- 抢占模式（preempt）：确保优先级最高的设备始终成为Master
- 主机网关地址：VRRP指定的虚拟IP地址
- 虚拟MAC地址：VRRP虚拟路由器的MAC地址

## VRRP基本配置

必选
- 添加VRRP组接口的虚拟IP地址
- 设置VRRP组的设备优先级
```shell
vrrp vrid VRID virtual-ip IP地址 # 创建VRRP组，并设置VRID和虚拟IP地址
vrrp vrid VRID priority 优先级 # 设置当前设备在VRID组下的VRRP优先级
```

可选
- 设置VRRP组的抢占模式、延迟时间（如果设备正忙，则延迟抢占）
- 设置监视指定端口
	- VRRP只能感知所在端口的状态变化，需要设置监视指定端口来判断路由器是否出现故障
```shell
vrrp vrid VRID preempt-mode timer delay 10 # 设置抢占模式，抢占延迟10秒
vrrp vrid VRID track interface 接口号 reduced 30 # 设置监视指定端口，若端口失效则优先级降低30
```
- 显示VRRP状态 `display vrrp`
- 显示VRRP详细信息 `display vrrp verbose`
- 查看VRRP的**报文收发统计信息** `display vrrp statistics`
### 创建多个VRRP组

通过创建多个VRRP组，每个物理路由器在不同VRRP组中都可以充当Master转发数据
- 充分利用路由器资源
- 实现负载均衡
![[Pasted image 20260425135327.png]]

### 心跳线

心跳线作用：在两台路由器之间部署一条心跳线，**用于传递VRRP协议报文**
![[Pasted image 20260425135713.png]]

![[Pasted image 20260425135720.png]]