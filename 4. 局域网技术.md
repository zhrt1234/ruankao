
## 局域网定义
### CSMA/CD协议

![[Pasted image 20260413172517.png]]
- CSMA载波监听多路访问
- CD碰撞检测
- **碰撞窗口**：往返时延2t
- **传输时延要大于等于碰撞窗口**： `传输时延 ≥ 2 * 传播时延`
	- 以太网最小间隔**9.6us**
- **最短帧长**：`帧长 ≥ 2 * (信道长度 / 传播速率) * 传输速率`
	- 以太网最短帧长**64Byte**
- **再发生冲突的概率**：第n次冲突后，再发生冲突的概率为`1/2^n`(n <= 10)

### MAC地址和以太网帧结构

![[Pasted image 20260413172530.png]]
- MAC地址长度 **6Byte**
- 数据链路层MTU：**1500Byte**
- 校验FCS：**4Byte**

### 以太网传输介质

![[Pasted image 20260413173227.png]]
- 双绞线
	- UDP非屏蔽双绞线
	- STP屏蔽双绞线
	- **距离小于100m**
	- 测量指标：**近端串扰**、衰减限值、回波损耗值
- 光纤
	- 多模光纤
		- 传输方式：全反射
		- 纤芯直径大
		- **最大距离550m**
	- 单模光纤
		- 传输方式：接近直线
		- 纤芯直径小
		- **距离几十公里**
	- 测量指标：**波长窗口参数**、光衰减限值、光回波损耗限制
	- 光纤损耗系数 `α = (10 / L) * log10(P0 / PL)`
		- 单位`dB/km`
		- L位光纤长度
		- P0为输入光功率
		- PL为传输距离L后光功率

![[Pasted image 20260413173911.png]]
光传输设备

### 高速以太网

以太网命名规则
- T：非屏蔽双绞线UTP
- C：屏蔽双绞线STP
- F：光纤
- SX(Short X)：多模光纤
- LX(Long X)：单模光纤

**各类线缆长度**
- STP：25m
- UTP：100m
- 多模光纤：550m
- 单模光纤：超过550m

**各类以太网（要背）**
- 快速以太网100Mbps
	- 半双工、全双工
	- **802.3u标准**
	- **100Base-T4**：3类双绞线、8B6T编码、100m
	- **100Base-T2**：3类双绞线、PAM5x5编码、100m
	- **100Base-TX**：5类双绞线、4B/5B编码、100m
	- **100Base-FX**：单模光纤或多模光纤、4B/5B编码、2000m
- 千兆以太网1000Mbps
	- 半双工、全双工
	- **802.3z标准**
		- **1000Base-SX**：多模光纤、550m
		- **1000Base-LX**
			- 多模光纤550m
			- 单模光纤5000m
		- **1000Base-CX**：2对STP、25m
	- **802.3ab标准**
		- **1000Base-T**：4对5类UTP、100m
- 万兆以太网10000Mbps
	- 全双工
	- **802.3ae标准**
	- 10GBASE-SR：多模光纤、300m
	- 10GBASE-LR：单模光纤、10km
	- 10GBASE-ER：单模光纤、40km
	- 10GBASE-T：4对6类UTP、100m

### 交换式以太网

![[Pasted image 20260413175958.png]]
- MAC地址学习：**帧中源MAC与端口的映射关系**
- MAC地址表中没有对应目的MAC地址时，以**广播的形式转发数据帧**
- 交换机MAC地址表老化时间为**300s**

交换机总结
- 交换机各个接口独享带宽，可以**隔离冲突域**
- 全双工100BASE-TX交换机，一个接口最大通信速率为**200Mbps**
- 交换机转发方式
	- **直通转发**
		- 接收并检测到目的地址字段就立刻转发
		- **交换效率高**
		- **无差错检测能力**
	- **存储转发**
		- 先存储再进行CRC校验，再转发
		- **具有差错检测能力**
		- **转发效率低**
	- 无碎片转发
		- 过滤小于**64Byte**的冲突碎片
- 交换机分类
	- 二层交换机：根据MAC地址进行交换
	- 三层交换机：根据网络层IP地址进行交换
		- 具有路由功能，但不等于路由器

### 冲突域和广播域

![[Pasted image 20260413180504.png]]
- 冲突域：所有节点竞争同一带宽，一个节点发送的报文，其余节点都能收到
- 广播域：同一广播域内的主机都能收到广播报文

### Eth-trunk(以太网链路聚合)

![[Pasted image 20260413201240.png]]
- 链路聚合原理
	- 将多条以太网物理链路捆绑成一条逻辑链路
- **链路聚合作用**
	- **提高链路带宽**
	- **提高链路可靠性**
	- **提供负载均衡**
	- **节约组网成本**
- 实现方式
	- 手动配置Eth-trunk
	- **LACP链路聚合控制协议，IEEE802.3ad**![[Pasted image 20260413202935.png]]
		- **链路聚合控制协议数据单元LACPDU**
			- 设备优先级
			- MAC地址
			- 接口优先级
			- 接口号
- 聚合规则
	- 成员接口的带宽按照聚合组中**成员接口的最小速率**计算
	- 基于包的复杂均衡![[Pasted image 20260413203037.png]]
		- 可能导致数据帧到达对短时间不一致，从而引起数据乱序
	- 基于流的负载均衡
		- 保证同一数据流的数据帧在同一条物理链路转发
	- 基于报文的IP地址的负载均衡
	- 基于MAC地址的负载均衡

![[Pasted image 20260418232156.png]]
错题
### 堆叠和级联

![[Pasted image 20260413203612.png]]
![[Pasted image 20260413203747.png]]

#### 堆叠（案例题->简答题）
- 堆叠技术包括iStack和集群CSS
	- **接入、汇聚层的交换机使用堆叠技术（istack）**
	- **核心层交换机使用集群技术（CSS）**
- **堆叠技术的优缺点**
	- 提升系统可靠性
	- 提升交换机的背板带宽
	- 增加端口密度
	- 在逻辑交换机之间可使用链路聚合技术，无需部署STP、VRRP就可以实现高可靠性和高链路利用率
	- 缺点
		- 堆叠需要同厂商设备使用专门的堆叠线缆和模块
		- 堆叠设备的数量也有所限制
		- 堆叠模式不支持即插即用，需要进行负载的配置
- 堆叠交换机的角色
	- **主交换机Master**：管理整个堆叠，堆叠系统中只有一台主交换机
	- **备交换机Standby**：主交换机的备份，堆叠系统中只有一台备交换机
	- **从交换机Slave**：业务转发
- 角色选取原则：**MAC地址越小优先级越高**
- 堆叠实现方式
	- 堆叠卡堆叠：使用专用的堆叠线缆、堆叠插卡
	- 业务口堆叠：使用普通线缆连接物理成员端口
- 集群CSS实现方式![[Pasted image 20260413204419.png]]

##### 案例题

![[Pasted image 20260501234903.png]]
![[Pasted image 20260501234914.png]]
6）汇聚层
7）核心层
8）接入层
- **核心层交换机负责提供高速可靠的干线连接**
- **汇聚层交换机提供安全可靠的访问策略，负责连接核心层和接入层**
- **接入层交换机负责连接终端**


### 以太网供电PoE

- **空闲脚供电：4、5脚正极，7、8脚负极**
- **数据脚供电：1、2脚同极，3、6脚同极**

## VLAN（案例题）
## GVRP协议


## 生成树协议STP（案例题）

![[Pasted image 20260416163507.png]]

- **生成树协议只需要部署在二层网络，无需部署在三层网络**
- 生成树协议标准
	- STP：**IEEE802.1D**
	- 快速生成树协议RSTP：**IEEE802.1W**
	- 多生成树协议MSTP：**IEEE802.1S**
- BPDU桥协议数据单元
	- **配置BPDU**：用于生成树协议计算的报文
	- **TCN BPDU**：用于在拓扑发生变化时发送的报文
- **网桥ID（BID）**
  ![[Pasted image 20260416000425.png]]
	- **BID越小，优先级越高**
	- 优先级取值范围：`0~65535`
	- **默认优先级**：`32768`
	- **网桥优先级一定是4096的倍数**
- **端口ID**：端口优先级 + 端口号
	- **默认端口优先级**： `128`
### 生成树协议算法

- **选举根桥**（Root Bridge，RB）：在交换网络中**选举BID最小的交换机**作为根桥
- **选举根端口**：在每个**非根桥上**选举一个**根端口**
	- **根端口是离根桥开销最小的端口**
	- **选举根端口的依据**
		- 本端口到根网桥路径成本最低
		  ![[Pasted image 20260416001310.png]]
		- 本端口直连的网桥ID最小
		  ![[Pasted image 20260416001350.png]]
		- 本端口直连的端口ID（端口优先级+端口号）最小
		  ![[Pasted image 20260416001414.png]]
- **选举指定端口**：指定端口是在每条物理链路上选出一个端口
	- **根桥RB上的端口都是指定端口**
	- **根端口的对向一定是指定端口**
	- 在没有根端口的连路上，选举指定端口
		- 本端口所在网桥到根网桥路径成本最低![[Pasted image 20260416002413.png|400]]
		- 本端口所在网桥的BID值较小![[Pasted image 20260416002521.png|400]]
- **阻塞非指定端口**
	- 阻塞交换机上所有非根端口、非指定端口

### 端口状态变化

![[Pasted image 20260416154048.png]]
**端口状态变化**

![[Pasted image 20260416002146.png]]
端口状态表

### 故障恢复

![[Pasted image 20260416154240.png]]
**根桥故障恢复，非根桥会在BPDU报文失效后重新选举，恢复时间50s**
- SW1发生故障，停止发送BPDU报文
- 非根桥节点SW2和SW3等待Max Age（20s）计时器失效，此时之前的BPDU报文失效，并得知上游出现故障
- 非根桥节点互相发送BPDU重新选举新的根桥
- 重新选举后，SW3的A端口经过两个forward delay（15s）时间后恢复转发状态

![[Pasted image 20260416154903.png]]
**直连链路故障恢复，恢复时间30s**
- 当交换机SW2检测到根端口的链路发生故障，其备用端口经过两个forward delay（15s）时间进入转发状态
- 此时备用端口变为根端口

![[Pasted image 20260416155206.png]]
**非直连链路故障恢复，恢复时间50s**

## RSTP快速生成树协议

- RSTP端口角色
	- 根端口
	- **替代端口（Alternate）：根端口的备份**
	- 指定端口
	- **备用端口：指定端口的备份**
- 端口状态
	- **丢弃**（Discarding）
	- **学习**（Learning）
	- **转发**（Forwarding）
- RSTP收敛机制
	- **边缘端口机制**（Edge Port）
		- 边缘端口与用户终端相连，可以由 Discarding状态转换到 Forwarding状态
	- **根端口快速切换机制（替代端口）**
	- **指定端口快速切换机制（备份端口）**
	- **P/A机制**（Proposal/Agreement机制）
		- 加速选举
- RSTP保护机制 
	- **BPDU保护**
	  ![[Pasted image 20260416161405.png|322]]
		- 正常情况下，边缘端口不会收到BPDU报文
		- 当边缘端口收到BPDU时，交换机会自动将变换端口设置为非边缘端口，并重新计算生成树，从而引起网络震荡
		- 开启BPDU保护功能后，如果边缘端口收到BPDU，**边缘端口将被error-down，同时通知网管系统**
	- **根保护**
		- 确保根桥角色不会因为网络问题而改变
	- **环路保护**
	- **TCN-BPDU保护**
		- 防御TC-BPDU报文攻击，避免频繁删除MAC地址表项
		- **配置在单位时间内，处理TC-BPDU报文的次数**
		- 对超出阈值的报文，定时器到期后，设备统一处理一次

## MSTP多生成树协议（考的最多的生成树协议）

- MSTP可以将一个或多个VLAN映射到一个Instance（实例）
	- 基于Instance计算生成树
	- 映射到同一个Instance的VLAN共享同一颗生成树
	- Instance ID取值范围 `0~4096`

## 无线局域网

### 无线局域网标准

![[Pasted image 20260507202016.png]]

- 2.4GHz总共有14个信道（**国内13个信道**），每个**信道带宽22MHz**，相邻**信道间隔5MHz**

### CSMA/CA协议

- 载波监听多路复用/冲突避免协议
- 解决**隐蔽站问题、暴露站问题**
- **监听到空闲后，等待一个帧间间隔IFS，再发送数据**
	- 帧间间隔时间长度：**SIFS < PIFS < DIFS < EIFS**
	- 时间越短，优先级越高
	- SIFS：服务访问点的应答
	- PIFS：**点对点**服务访问点轮询（PCF）
	- DIFS：**分布式**协调功能竞争访问（DCF）
	- EIFS：出错

### WIFI安全（案例题）
#### 认证方式
![[Pasted image 20260507212245.png]]

#### 加密方式
![[Pasted image 20260507212318.png]]

### AC + AP

- AP分类：
	- 瘦AP（FIT AP）
		- 需要配合AC使用，由AC统一管理
		- 适合大中型企业
	- 胖AP（FAT AP）
		- AP独立工作，需要单独配置
		- 适合家庭、微型门店
- 供电方式：
	- 电源适配器供电
	- POE供电
- **CAPWAP隧道**
	- 维护AC与AP之间的状态
	- AC通过CAPWAP隧道对AP进行管理、业务配置下发
- 组网方式
	- 二层组网
	- 三层组网
	- **直连式组网**：数据经过AC到达上层网络
		- 对应于隧道转发方式
	- **旁挂式组网**：数据不经过AC
		- 对应于直接转发方式
- 业务数据转发方式
	- **隧道转发方式**：数据通过CAPWAP隧道到达AC，由AC转发到上层网络
	- **直接转发方式**：数据直接发到上层网络
- AP注册方式
	- **DHCP方式：AP通过DHCP响应报文的Option43，获取AC的IP地址**
	- DNS方式

#### AC+AP配置流程（案例题）

AC配置
- **划分AP组**
- **配置AP上线**
	- 配置AC的DHCP服务
	- 配置认证方式
	- 配置CAPWAP隧道
- **配置无线业务**
	- 创建安全模板、SSID模板、VAP模板
	- 配置SSID
	- 配置加密方式
	- 配置业务VLAN
- **配置信道、发射功率**

AP配置
- 为业务网段配置DHCP

## 案例题错题

![[Pasted image 20260501234427.png]]
- **WIFI6的标准是802.11ax，数据速率9.6Gbps**
- **WPA2采用AES加密算法，密钥长度至少为128位**


![[Pasted image 20260502002842.png]]
- **基于端口划分：固定办公**
- **基于MAC地址的VLAN划分：移动办公**