![[Pasted image 20260426152439.png]]

## NAT的实现方式

- 静态NAT
- 动态NAT
- 端口多路复用（PAT）
- Easy IP
- NAT Server

### 静态NAT（不考）

![[Pasted image 20260426144626.png]]

### NAT地址池

- 创建NAT地址池，并指定IP范围 `nat address-group 地址池名 起始IP地址 终止IP地址`

```shell
nat address-group 地址池名 地址池ID # 创建NAT地址池，并进入NAT地址池视图
	section 起始IP地址 终止IP地址 # 指定起始地址、终止地址
	route enable # 为NAT地址池中的地址生成OPR路由，类似于黑洞路由，可以防止路由环路
	mode pat # 指定NAT模式为pat
```
### NAPT端口多路复用（PAT）（考的多）

内网地址 -> 外网地址 + 端口号，一个外网地址对应多个内网地址
![[Pasted image 20260426154918.png]]
- 启用动态NAT `nat outbound ACL编号 [address-group 地址池名] [no-pat]`
	- `address-group` 指定NAT使用的外网地址池，如果不指定，**默认使用端口的IP地址**
	- `no-pat` **取消端口多路复用**，私网和公网地址一对一映射
- 查看NAT配置结果 `display nat outbound`
```shell
nat address-group ag1 122.1.2.1 122.1.2.3 # 设置外网地址池
acl 2000
	rule 5 permit source 192.168.1.0 0.0.0.255 # 设置允许通过的内网IP
	quit
interface g0/0/1
	nat outbound 2000 address-group ag1 # 启用动态NAT，外网地址池为ag1
```

### Easy IP（考的少）

- 使用场景
	- 小型局域网访问Internet的情况
	- 出接口通过拨号方式获得临时公网IP以供内部主机访问Internet
- 配置方式 `nat outbound ACL编号`
	- 不需指定外网地址池，默认使用出接口的IP地址
![[Pasted image 20260426155712.png]]

### NAT server（考的多）

![[Pasted image 20260426155842.png]]
- NAT Server的作用：让外网用户可以访问内网服务器
- 配置NAT Server `nat server [protocol 协议名] global 外网地址 [端口号] inside 内网地址 [端口号] [acl ACL编号]`
	- `global 外网地址 [端口号]` 指定服务器经NAT转化后的外网地址和端口
		- 指定一个范围的外网地址， `global 起始外网地址 终止外网地址 [端口号]`
		- 使用 `current-interface` 表示当前端口的IP地址
	- `inside 内网地址 端口号` 指定服务器的内网地址和端口
		- 指定一个范围的内网地址， `inside 起始内网地址 终止内网地址 [端口号]`
	- `acl ACL编号` 设置访问控制列表
- 取消NAT Server配置 `undo nat server`

在公网接口g0/0/1上配置NAT Server，将内网IP映射为当前接口的IP地址
```shell
interface g0/0/1
	nat server protocol tcp global current-interface 8080 inside 192.168.20.2 8080
```

将内网服务器的公网 `1.1.1.1` 地址转换为内网的 `192.168.0.1` ，仅允许 `2.2.2.2` 的用户访问内网服务器
```shell
acl 2001
	rule 5 permit source 2.2.2.2 0
	quit
interface g0/0/1
	nat server protocol tcp global 1.1.1.1 8080 inside 192.168.0.1 8080 acl 2001
```


## 防火墙上的NAT配置

- 防火墙上配置NAT的流程
	- 配置端口IP
	- 配置出口路由、回程路由
	- 配置区域（trust、untrust、dmz）
	- 配置 `security-policy` 允许区域间互访
	- 创建NAT（PAT模式）地址池
	- 配置 `nat-policy` NAT策略
- 配置指令
```shell
nat-policy # 进入NAT策略配置视图
	rule name 规则名 # 创建规则
		source-address IP地址 {掩码长度 | mask 子网掩码} # 配置NAT策略的源IP地址
		source-zone 区域名 # 配置NAT策略的源区域，如trust区域
		
		destination-address IP地址  {掩码长度 | mask 子网掩码} # 配置NAT策略的目的IP地址
		destination-zone 区域名 # 配置NAT策略的目的区域
		
		action source-nat address-group 地址池名 # 对源地址进行转换，并指定转换后的地址池
```

![[Pasted image 20260426145518.png]]
- 配置接口IP
![[Pasted image 20260426145552.png]]
- 配置出口路由、回程路由
![[Pasted image 20260426145725.png]]
- 配置防火墙安全区域
![[Pasted image 20260426145806.png]]
- 配置 `security-policy`，允许遇见互访
![[Pasted image 20260426145858.png]]
- 创建NAT地址池
![[Pasted image 20260426150817.png]]
- 配置 `nat-policy`
![[Pasted image 20260426150905.png]]

### 黑洞路由

为了防止因NAT引起的路由环路，需要在防火墙配置黑洞路由 `ip route-static NAT转换后的IP地址 32 NULL 0`
- 当NAT地址池地址与出接口地址不在同一网段时，必须配置黑洞路由
- 当NAT地址池地址与出接口地址在同一网段时，建议配置黑洞路由
- 当地址池地址与出接口地址一致时，不会产生路由环路，不需配置黑洞路由
![[Pasted image 20260426162928.png]]
### 内网主机访问内网服务器的外网地址（案例题）

![[Pasted image 20260426151619.png]]
- 不能通信：**防火墙没有配置源地址的NAT转换**
	- 内网主机发送请求源IP `192.168.1.3`，目的IP `11.11.11.6`
	- 路由器收到请求，通过NAT将目的IP转换为 `192.168.1.2`
	- 内网服务器收到请求，源IP `192.168.1.3` ，目的IP `192.168.1.2`
	- 内网服务器发送回复报文，源IP `192.168.1.2`，目的IP `192.168.1.3`
	- 交换机直接转发给内网主机
	- 内网主机发现源IP为 `192.168.1.2`，并不是 `11.11.11.6`，丢弃报文
- **需要配置源地址的NAT转换，把内网主机的内网IP地址转换为外网IP地址，再与内网服务器的外网IP地址通信，从而保证请求报文的目的地址和回应报文的源地址保持一致。**

原因：防火墙没有配置NAT源地址转换，导致内网用户使用内网IP地址访问server，server处理完请求后使用server的内网IP作为源地址发送回应报文，此时内网用户收到的回应报文源IP地址与请求报文的目的IP地址不一致，直接丢弃报文，导致无法通信。

解决方案：在防火墙上配置NAT源地址转换，将内网用户的IP地址转换为公网地址再发送给server，server的回应报文同样会发送给防火墙，再将源地址转换为公网地址发送给内网用户，此时回应报文的源地址和请求报文的目的地址一致，内网用户的主机不会丢弃报文。
### 防火墙的源进源出功能（案例题）

- 源进源出：流量从哪个端口进来，就从哪个端口出去
```shell
interface 端口号
	redirect-reverse next-hop 网关IP地址 # 启用源进源出
	gateway 网关IP地址 # 设置网关IP地址
```

![[Pasted image 20260426152336.png]]
![[Pasted image 20260426152414.png]]
- **原因：数据包的往返路径不一致，例如从ISP1路径通过防火墙访问数据，服务器回应时，数据包到达防火墙时，因ISP2路径的路由优先级更高，导致数据包从ISP2返回用户主机。**
- **解决方案：在防火墙上配置源进源出功能。**

## 案例题

![[Pasted image 20260509104605.png]]
没有进行NAT转换
解决方法：在FW配置NAT转换策略