
## 端口隔离

- 作用：同一VLAN内端口之间进行隔离。**同组隔离，不同组不隔离。**
- `port-isolate mode [l2(二层隔离，默认) | all(三层隔离)]`
	- `l2`：二层隔离，可以通过VLANIF、三层网关通信
	- `all`：二层隔离+三层隔离，彻底无法通信

![[Pasted image 20260414114655.png]]

### 三层隔离

```shell
[LSW2]port-isolate mode all
[LSW2]interface g0/0/1
[LSW2-GigabitEthernet0/0/1]port link-type access # 设置接口属性
[LSW2-GigabitEthernet0/0/1]port-isolate enable group 1
[LSW2-GigabitEthernet0/0/1]quit
[LSW2]interface g0/0/2
[LSW2-GigabitEthernet0/0/2]port link-type access # 设置接口属性
[LSW2-GigabitEthernet0/0/2]port-isolate enable group 1
[LSW2-GigabitEthernet0/0/2]quit
```
![[Pasted image 20260414114431.png]]
**PC4无法与PC5通信，可以与PC6通信**
### 二层隔离

```shell
[LSW2]port-isolate mode all
[LSW2]interface g0/0/1
[LSW2-GigabitEthernet0/0/1]port-isolate enable group 1
[LSW2-GigabitEthernet0/0/1]quit
[LSW2]interface g0/0/2
[LSW2-GigabitEthernet0/0/2]port-isolate enable group 1
[LSW2-GigabitEthernet0/0/2]quit

# 创建VLANIF1，提供group内通信
[LSW2]interface vlanif1
[LSW2-Vlanif1]ip address 10.51.90.254 255.255.255.0
[LSW2-Vlanif1]arp-proxy inner-sub-vlan-proxy enable # 同一VLAN内开启ARP代理
```
**注意：不同VLAN之间开启ARP代理**`inter-sub-vlan-proxy`

![[Pasted image 20260414115543.png]]
**创建VLANIF1后，PC4可以与PC5通信**

### 不同VLAN之间开启ARP代理

```shell
# 创建VLANIF1
[LSW2]interface vlanif1
[LSW2-Vlanif1]ip address 10.51.90.254 255.255.255.0
[LSW2-Vlanif1]arp-proxy inter-sub-vlan-proxy enable # 不同VLAN之间开启ARP代理

# 创建VLANIF2
[LSW2]interface vlanif2
[LSW2-Vlanif1]ip address 10.51.91.254 255.255.255.0
[LSW2-Vlanif1]arp-proxy inter-sub-vlan-proxy enable # 不同VLAN之间开启ARP代理

# PC4
IP 10.51.90.14/24
网关 10.51.90.254 # VLANIF1

# PC6
IP 10.51.91.16/24
网关 10.51.91.254 # VLANIF2
[LSW2-GigabitEthernet0/0/3]port default vlan 2 # PC6在vlan2下
```

![[Pasted image 20260414161019.png]]

## 镜像端口

- `observe-port 观察端口ID interface 目标端口` 指定一个端口作为观察端口![[Pasted image 20260414164048.png]]
- `port-mirroring to observe-port 1 inbound` 将源端口流量镜像到观察端口![[Pasted image 20260414164550.png]]
	- inbound：入站流量
	- outbound：出站流量

## 交换机安全相关配置

### 静态MAC

`mac-address static MAC地址 接口名 接口ID vlan VLAN_ID`
- 例如 `mac-address static 0005-0005-0005 g 0/0/2 vlan 2`
![[Pasted image 20260415111853.png]]
### 黑洞MAC地址

为防止黑客通过MAC地址攻击用户设备或网络，将非信任用户的MAC地址配置为**黑洞MAC地址**。当设备收到目的MAC或源MAC地址为黑洞MAC地址的报文，直接丢弃。

`mac-address blackhole MAC地址 [vlan VLAN_ID]`
- 例如 `mac-address blackhole 0005-0005-0005 vlan 2`

![[Pasted image 20260415112604.png]]
![[Pasted image 20260415112228.png]]
### MAC表

- 配置MAC表老化时间 `mac-address aging-time 老化时间`
- 关闭基于接口的MAC地址学习 `mac-address learning disable [action {discard | forward}]`
	- 需要先进入某个接口
	- 当MAC地址与地址表不匹配时，可选择丢弃discard或转发forward，**默认为转发**
	- 例如 `[Huawei-GigabitEthernet0/0/1] mac-address learning disable discard`
- 关闭基于VLAN的MAC地址学习 `mac-address learning disable`
	- 需要先进入某个vlan
	- 同时配置基于接口和基于VLAN的禁止MAC地址学习功能时，**VLAN优先级高于接口**
	- 例如 `[Huawei-vlan2] mac-address learning disable`
- 配置限制MAC地址学习数 `mac-limit maximum 最大值`
	- 需要先进入**某个接口或VLAN**
- 配置当MAC地址数达到限制后，对报文采取的动作 `mac-limit action { discard | forward}`
	- **默认为discard丢弃**
- 配置当MAC地址数达到限制后是否告警 `mac-limit alarm {disable | enable}`
	- **默认为enable告警**

![[Pasted image 20260414201258.png]]
![[Pasted image 20260415114506.png]]

### 端口安全配置（案例题）

端口安全：通过将接口学习到的动态MAC地址转换为安全MAC地址，阻止非法用户通过本接口通信，增强设备安全性。
- **安全静态MAC地址**
	- 手工配置
	- 不会老化，手动保存配置后重启设备不丢失
- **Sticky MAC地址**
	- 打开Sticky MAC功能后转换得到的MAC地址
	- 不会老化，手动保存配置后重启设备不丢失
- **安全动态MAC地址**
	- 未打开Sticky功能时转换的MAC地址
	- 默认情况不会老化，设备重启后丢失

**以下操作均需要在端口下配置**
- 打开端口安全功能 `port-security enable`
- 配置端口安全动态MAC学习限制数量 `port-security mac-max-num 最大值`
	- **默认最大值为1**
- 配置端口安全静态MAC地址 `port-security mac-address MAC地址 vlan VLAN_ID`
- **配置端口安全保护动作** `port-security protect-action {protect | restrict | shutdown}`
	- `protect` 丢弃
	- `restrict` 丢弃并上报
	- `shutdown` 丢弃、上报、关闭端口
	- **默认为 restrict**
- 打开端口Sticky MAC功能 `port-security mac-address sticky`
- 配置端口Sticky MAC学习限制数量 `port-security max-mac-num 最大值`
	- **默认最大值为1**

![[Pasted image 20260414203229.png]]

## MAC地址漂移检测（案例题）

MAC地址漂移：交换机上一个VLAN内有两个端口学习到同一个MAC地址，后学习到的MAC地址表项覆盖前一个表项。出现这种情况一般为
- 网络中存在环路，
	- 解决方法是部署STP生成树协议，消除二层环路![[Pasted image 20260414203534.png]]
- 网络中存在攻击行为，解决方法
	- 配置端口MAC地址学习优先级
	- 配置不允许相同优先级端口MAC地址漂移![[Pasted image 20260414204010.png]]

- 配置端口MAC地址学习优先级 `mac-learning priority 优先级`
- 配置MAC地址漂移检测功能 `mac-address flapping detection`
- 配置MAC地址漂移检测间隔 `mac-address flapping aging-time 时间`
- 配置MAC地址检测到漂移后的动作 `mac-address flapping action error-down`
- 配置error-down自动恢复时间 `error-down auto-recovery cause mac-address-flapping interval 500`

![[Pasted image 20260414204350.png]]

## 流量抑制

- 开启流量抑制`suppression mode by-packets`
- 配置未知单播流量抑制 `unicast-suppression 百分比`
	- 例如 `unicast-suppression 80`
- 配置未知组播流量抑制 `multicast-suppression 百分比`
- 配置广播流量抑制 `broadcast-suppression 百分比`

![[Pasted image 20260414211017.png]]
流量配置应配置在流出接口
## 风暴控制

相比流量抑制，风暴控制有**惩罚措施**
- 设置风暴控制白名单 `storm-control whitelist protocol 协议名`
	- 例如 `storm-control whitelist protocol arp-request`
- 设置风暴控制阈值 `storm-control {unicast | multicast | broadcast} min-rate 最小速率 max-rate 最大速率`
	- 例如 `storm-control unicast min-rate 1000 max-rate 2000`
- 设置风暴控制检测时间间隔 `storm-control interval 时间`
- 设置惩罚动作，当超过阈值时阻塞端口 `storm-control action block`
- 设置是否开启报警 `storm-control {enable | disable} trap`

![[Pasted image 20260414210141.png]]

## IPSG利用绑定表

IPSG利用绑定表用于匹配检查二层端口上收到的IP报文，只有匹配绑定表的报文才允许通过。常见绑定表有**静态绑定表**和**DHCP Snooping动态绑定表**，需要满足以下所有条件
- 源IP地址
- 源MAC地址
- 所属VLAN
- 入接口
![[Pasted image 20260414211412.png]]

- 绑定IP和MAC地址、VLAN、端口 `user-bind static ip-address IP地址 mac-address MAC地址 [vlan VLAN_ID] [interface 端口号]`
- 打开端口的IPSG功能 `ip source check user-bind enable`
- 打开端口的IP报文检查告警功能 `ip source check user-bind alarm enable`
- 设置告警阈值，当丢弃报文达到100个时告警 `ip source check user-bind alarm threshold 100`
![[Pasted image 20260414211214.png]]

## MACsec

MACsec定义了基于以太网的数据安全通信方法，通过逐条设备之间数据加密，保证数据传输安全性，对应标准为802.1AE
![[Pasted image 20260414211152.png]]