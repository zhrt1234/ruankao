
## 路由策略的定义

![[Pasted image 20260425192438.png]]

## 路由控制工具

### 路由匹配工具

- 访问控制列表ACL
	- 只匹配路由网络号 + 通配符，无法匹配子网掩码
	- `rule [RULE_ID] {permit | deny} [source IP地址 通配符]`
- IP前缀列表ip-prefix
	- 可以匹配路由 + 前缀长度（子网掩码）
	- `ip ip-prefix 前缀名 [index INDEX_ID] {permit | deny} IP地址 前缀长度`

![[Pasted image 20260425220128.png]]
只将DeviceB中192.168.0.0/16导入OSPF中
```shell
# ACL配置方式
acl 2001
	rule permit source 192.168.0.0 0.0.255.255
	quit
	
	
# ip-prefix配置方式
ip ip-prefix CSAI permit 192.168.0.0 16
```

### filter-policy

filter-policy能用于ISIS、OSPF、BGP等协议，用于对路由过滤
- 对协议接受的路由过滤 `filter-policy {ACL编号 | ip-prefix 前缀名} import`
- 对协议发布的路由过滤 `filter-policy {ACL编号 | ip-prefix 前缀名} export`

#### 案例1

![[Pasted image 20260425193242.png]]
![[Pasted image 20260425193319.png]]
![[Pasted image 20260425193346.png]]
![[Pasted image 20260425193427.png]]
![[Pasted image 20260425193436.png]]
![[Pasted image 20260425193520.png]]
![[Pasted image 20260425193737.png]]
![[Pasted image 20260425193853.png]]

#### 案例2

- 市场部不能访问财务部、研发部
- 公司总部不能访问研发部
![[Pasted image 20260425221425.png]]
![[Pasted image 20260425221506.png]]

### route-policy

- route-policy功能
	- 匹配给定路由信息的某些属性
	- 在条件满足时改变路由信息的属性
- 匹配规则
  ![[Pasted image 20260425200804.png|505]]
	- 在if-match匹配失败，可以继续匹配下一个node
	- 在node中被deny，则结束匹配

#### route-policy配置（类似于PBR）

```shell
route-policy 策略名 {permit | deny} node 节点号 # 创建路由策略
	if match xxx # 条件语句
	apply xxx # 执行语句
```
- 节点号：取值范围 `0~65535`
- if-match条件语句
	- `acl` 匹配ACL
	- `cost` 匹配路由信息的cost
	- `interface` 匹配路由信息的出接口
	- `ip-prefix` 匹配前缀列表
- apply执行语句
	- `cost` 设置路由的cost
	- `cost-type {type-1 | type-2` 设置OSPF的开销类型E1/E2
	- `ip-address next-hop` 设置下一跳
	- `preference` 设置路由协议优先级
	- `tag` 设置路由信息的标记

#### 路由过滤
- 允许192.168.1.0/24
```shell
ip ip-prefix prefix1 permit 192.168.1.0 24 # 允许192.168.1.0/24
route-policy rp1 permit node 10 # 允许符合条件的路由
	if-match ip-prefix prefix1
	apply permit
```
- 在当前策略的node 10中，拒绝192.168.1.0/24，继续匹配当前策略的下一个node，由于没有下一个node，因此192.168.1.0/24被路由策略拒绝
```shell
ip ip-prefix prefix2 deny 192.168.1.0 24 # 拒绝192.168.1.0/24
ip ip-prefix prefix2 permit 0.0.0.0 # 允许所有匹配
route-policy rp2 permit node 10 # 允许符合条件的路由
	if-match ip-prefix prefix2
	apply permit
```
- 192.168.1.0/24匹配成功，但被路由策略拒绝
```shell
ip ip-prefix prefix3 permit 192.168.1.0 24 # 允许192.168.1.0/24
route-policy rp3 deny node 10 # 拒绝符合条件的路由
	if-match ip-prefix prefix3
	apply permit
```

#### 案例1
![[Pasted image 20260425201235.png]]

#### 案例2 使用路由策略控制访问

![[Pasted image 20260425203400.png]]

#### 使用路由策略修改路由属性

![[Pasted image 20260425205519.png]]
```shell
ip ip-prefix external index 10 permit 192.168.1.0 24 # 添加外部路由的IP前缀
route-policy rp permit node 10 # 添加路由策略
	if-match ip-prefix external
	apply cost-type type-1 # 修改OSPF外部路由的cost-type
	quit
ospf
	import-route direct route-policy rp # 应用上面的路由策略
```

#### 路由引入错误

![[Pasted image 20260425210606.png]]
- OSPF引入路由的优先级是150，ISIS路由优先级是15
- RTB->RTC的路由优先级是150
- RTB->RTC->RTD->RTC的路由会变成ISIS的优先级15
- RTB和RTC之间产生路由回路
![[Pasted image 20260425210937.png]]![[Pasted image 20260425210943.png]]
![[Pasted image 20260425210950.png]]

## 案例题

![[Pasted image 20260425213658.png]]
![[Pasted image 20260425212515.png]]
没有关闭路由自动汇总功能，RIP协议将学习到的路由自动汇总为10.0.0.0/8
![[Pasted image 20260425212625.png]]
```shell
10.2.0.0/29 RIP 100 1 D 192.168.12.1 GigabitEthernet 1/0/1
10.2.0.8/29 RIP 100 1 D 192.168.12.1 GigabitEthernet 1/0/1
10.2.0.16/29 RIP 100 1 D 192.168.12.1 GigabitEthernet 1/0/1
10.2.0.32/29 RIP 100 1 D 192.168.12.1 GigabitEthernet 1/0/1
```
![[Pasted image 20260425213253.png]]