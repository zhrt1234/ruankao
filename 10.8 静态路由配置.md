
![[Pasted image 20260419204300.png]]
-  `ip route-static 目的IP 目的子网掩码 下一跳IP`
	- 添加静态路由，只指定下一跳
	- 路由表会地柜查询出接口，会增加路由器负担
-  `ip route-static 目的IP 目的子网掩码 出接口 下一跳IP`
	- 添加静态路由，同时指定出接口和下一跳
	- 等价于 
```
ip route-static 目的IP 目的子网掩码 下一跳IP
ip route-static 目的IP 出接口
```
  -  `ip route-static 0.0.0.0 0.0.0.0 下一跳IP`
	  - 配置默认路由，一般下一跳设置为网关
- `ip route-static 目的IP 目的子网掩码 null0`
- 配置黑洞路由，丢弃符合条件的报文

### 配置等价静态路由

配置两条目标相同、下一跳不同的静态路由，可实现负载均衡
  
```
ip route-static 目的IP 目的子网掩码 下一跳IP_1
ip route-static 目的IP 目的子网掩码 下一跳IP_2
```
![[Pasted image 20260419204512.png]]

### 配置浮动静态路由（案例题）

路由表中存在相同目标网络的路由条目时，根据路由条目优先级，将请求转发到相应端口，**另一条链路作为冗余**
```
ip route-static 目的IP 目的子网掩码 下一跳IP_1
ip route-static 目的IP 目的子网掩码 下一跳IP_2 preference 61
```

-  `display ip routing-table`时只能看到主路由
- `display ip routing-table protocol static` 可以看到所有静态路由，包括备份路由

题目
![[Pasted image 20260419205553.png]]![[Pasted image 20260419205653.png]]

![[Pasted image 20260419210212.png]]
- 10.0.2.0/24链路故障
- 其他网段报文发送至`路由器D`
- `路由器D`没有找到通往10.0.2.0/24的路由，通过默认路由发送给`路由器E`
- `路由器E`从路由表中发现报文应该转发给`路由器D`
- 此时会形成路由环路，虽然有TTL但也会消耗网络资源，因此可以配置黑洞路由，丢弃IP网段错误的报文