![[Pasted image 20260425162254.png]]

## ACL基本工作原理

- 设备可以通过应用ACL来实现**允许或阻止**报文通过
- 匹配规则：从前往后匹配
- **路由器ACL末尾默认允许，防火墙默认拒绝**
	- 路由器需要设置最小特权原则：`any any deny`
- ACL的功能
	- 提供安全访问
	- 防止网络攻击：封堵高危端口（8080端口）
	- 提高网络带宽利用率

## ACL的应用

![[Pasted image 20260425171431.png]]
- **出流量outboud**：从路由器接口离开的数据，已经被路由器处理
- **入流量inbound**：进入路由器接口的数据，将要被路由器处理
- **路由器自身产生的流量不会被ACL检测**

## ACL配置

- 基本ACL
	- 编号范围：**2000~2999**
	- **源IP地址**、**生效时间段**、分片信息
- 高级ACL
	- 编号范围：**3000~3999**
	- 源IP地址、目的IP地址、协议类型、源端口号、目的端口号、生效时间段
- **rule为deny时，需要加上permit rule以确保ACL不会拒绝所有流量**
### 基本ACL

```shell
acl [number] ACL编号(2000~2999) [match-order {auto | config}] # 配置ACL
	rule [RULE_ID] {permit | deny} [source {IP地址 通配符 | any}]
```
- acl指令
	- `number` 可以写，也可以不写，都会指定ACL编号
	- `match-order` 指定ACL的匹配顺序
		- `auto` 自动按照掩码的长度匹配，优先匹配长度更长的
		- `config` 按照ACL配置顺序匹配
- rule指令
	- `RULE_ID` 不指定时自动增长，默认步长为5，首条编号为5
	- **通配符**：0表示严格匹配，1表示任意，通常为反子网掩码

![[Pasted image 20260425172559.png]]
![[Pasted image 20260425190223.png]]
### 高级ACL

```shell
acl [number] ACL编号(3000~3999) [match-order {auto | config}] # 配置ACL
	rule [RULE_ID] {permit | deny} [协议] [source {IP地址 反子网掩码 | any}] [destination {IP地址 反子网掩码 | any}] [比较符 端口号]
```
- **协议**：TCP、UDP、ICMP、IP等
- **比较符**
	- `eq` 等于
	- `lt` 小于
	- `gt` 大于
	- `neq` 不等于
	- `range` 指定范围

![[Pasted image 20260425173150.png]]
![[Pasted image 20260425190238.png]]

### 基于时间的ACL（考的少）

- 创建一个时间段 `time-range 时间段名 {开始时间 to 结束时间 | from 开始时间 to 结束时间`
- 支持同一个时间段名配置多条时间段范围
	- 周期时间段之间、绝对时间段之间取并集
	- 周期时间段与绝对时间段取交集
![[Pasted image 20260425161412.png]]
![[Pasted image 20260425161521.png]]

### 命名的ACL

使用ACL名称代替ACL编号
- 基本命名ACL `acl name ACL名称 {basic | ACL编号} [match-order]`
- 高级命名ACL `acl name ACL名称 {advance | ACL名称} [match-order]`

例子
```shell
acl name aaa 2001
acl name bbb advance
```

### ACL部署的位置

![[Pasted image 20260425161849.png|511]]
- 高级ACL：更精确，靠近数据流的源
- 基本ACL：靠近数据流的目的地
