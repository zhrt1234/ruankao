
![[Pasted image 20260419171751.png]]
**路由转发**：路由器根据数据包的**目标IP地址**，在本地**路由表**中查找最优路径，并将数据包转发到下一跳设备的过程。
- 若有匹配路由条目，则依据条目中的出接口或下一跳等信息进行报文转发
- 若无匹配路由条目，则丢弃报文

### 路由来源

![[Pasted image 20260419171951.png]]

### 路由表

- 查看设备路由表 `display ip routing-table`
![[Pasted image 20260419172524.png|572]]
- `Destionation/Mask` 目的地址、掩码长度
- `Proto`：通过什么方式获得路由
	- Direct直连、static静态、RIP、OSPF
- `Pre` 优先级，数值越小越优先，同样的路由条目，选择**优先级数值更小的加入路由表**
	  ![[Pasted image 20260419200258.png|399]]
	- 直连路由：0
	- 静态路由：60
	- OSPF：10
	- IS-IS：15
	- RIP：100
	- BGP：255
- `Cost` 路由开销（度量值）
	- 跳数、链路带宽、链路延迟、链路使用率
- `Flags`：路由状态和特性
	- `D` 表示该路由已经被下载到**硬件转发信息库FIB**中，以加速数据包转发
	- `R` 表示该路由是动态路由协议学习到的
- `NextHop`：下一跳的IP地址
- `Interface`：出接口

- **路由表项类型**
	- 主机路由：掩码长度为32位，匹配单一IP地址
	- 子网路由：匹配一个子网
	- 默认路由：0.0.0.0，匹配所有IP地址
### 路由匹配原则

- 最长网络前缀匹配原则
- 路由协议优先级`Pre`最小
- `Cost`值最小

![[Pasted image 20260419210607.png]]
### FIB转发信息库

- FIB用来知道IP报文的转发
- 查看FIB转发信息库 `display fib`
![[Pasted image 20260419194906.png]]

### 路由迭代

- 路由必须有直连的下一跳才能指导转发
- 路由的下一跳可能不是直连的，可以通过**路由迭代**计算出下一跳的IP和对应的出端口
	- A->B->C，通过A->B计算得到A->C的下一跳是B，出端口是与B直连的端口
![[Pasted image 20260419195324.png]]
