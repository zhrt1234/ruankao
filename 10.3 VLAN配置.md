
**基于端口的划分考的较多**
- VLAN的流量是二层流量，到网关截止
- 从网关到外面是三层流量
## VLAN配置

### VLAN操作

- 创建VLAN `vlan VLAN_ID`
	- 批量创建VLAN `vlan batch [VLAN_ID1 to VLAN_ID2]`
		- 例如 `vlan batch 2 to 5`、`vlan batch 3 5 7`
- 通过ID删除VLAN `undo vlan VLAN_ID`
- 通过VLAN名称创建匿名VLAN、进入VLAN视图：`vlan vlan-name VLAN名称`
	- 名称区分大小写，长度1~31
- 通过名称删除VLAN `undo vlan vlan-name VLAN名称`

### 基于端口划分VLAN（考的较多）

- 配置端口类型 `port link-type {access | trunk | hybrid}`
	- access：连接终端，access端口只能有一个VLAN
	- trunk、hybrid：交换机之间连接，可以有多个VLAN

#### access端口设置

- 设置当前接口允许通过的VLAN `port default vlan VLAN_ID`
	- PVID是端口的默认VLAN
		- access端口：**PVID与端口的VLAN一致**
		- trunk端口：PVID根据需求配置，默认为**VLAN1**

#### trunk端口设置

- 配置trunk允许通过的VLAN `port trunk allow-pass vlan [VLAN_ID1 to VLAN_ID2 | all}`
	- `all` 一般不使用，允许通过所有VLAN
- 配置trunk的默认VLAN `port trunk pvid vlan VLAN_ID`
#### hybrid端口设置

- `port hybrid {tagged | untagged} vlan VLAN_ID`
	- `tagged` 数据包带标签
	- `untagged` 数据包不带标签

#### 批量配置

**只有access端口支持批量配置**

在GROUP视图下批量配置
- 创建端口组 `port-group GROUP_ID`
- 将端口加入端口组 `group-member [INTERFACE_ID1 to INTERFACE_ID2]`
	- 例如 `group-member g0/0/1 to g0/0/5`
- 设置端口类型为access `port link-type access`
- 设置端口VLAN `port default vlan VLAN_ID`

在VLAN视图下批量配置
```shell
vlan 10
port g0/0/1 to g0/0/5
```

#### VLANIF配置

- 创建vlanif `interface vlanif VLANIF_ID`
- 配置vlanif的IP `ip address IP地址 子网掩码`

![[Pasted image 20260418200551.png]]
方案1
![[Pasted image 20260418200537.png]]![[Pasted image 20260418200603.png]]
方案2：注意hybrid要设置PVID，因为默认PVID为vlan1
![[Pasted image 20260418200744.png]]
![[Pasted image 20260418200727.png]]

### 基于MAC地址划分VLAN

- 操作步骤
	- 进入VLAN视图
	- 指定MAC地址
	- 进入端口视图，开启基于MAC地址划分VLAN功能
- 将MAC地址与VLAN关联 `mac-vlan mac-address MAC地址 [priority 优先级]`
	- `priority` 设置VLAN的网络拥塞时的转发优先级，范围0~7，值越大优先级越高
- 配置端口，启用基于MAC地址划分VLAN `mac-vlan enable`
- 将端口从三层模式切换到二层模式 `portswitch`
- 切换回三层模式 `undo portswitch`
![[Pasted image 20260418202547.png]]
![[Pasted image 20260418201218.png]]

### 基于子网划分VLAN

**基于IP子网划分VLAN只能应用在类型为hybrid的接口**
- 操作步骤
	- 进入VLAN视图
	- 指定IP子网
	- 进入端口视图，开启基于IP子网划分VLAN功能
- 将IP子网和VLAN关联 `ip-subnet-vlan [IP_SUBNET_INDEX] ip IP地址 子网掩码 [priority 优先级]`
	- `IP_SUBNET_INDEX` 指定IP子网索引值，范围1~12，可以手动指定，也可以自动生成
	- `priority` 设置VLAN的网络拥塞时的转发优先级，范围0~7，值越大优先级越高
- 取消IP子网与VLAN关联 `undo ip-subnet-vlan IP_SUB_NET_INDEX [to IP_SUBNET_INDEX2]`
- 配置端口，启用基于IP子网划分VLAN `ip-subnet-vlan enable`
![[Pasted image 20260418202300.png]]![[Pasted image 20260418202336.png]]

## VLAN聚合

VLAN聚合：用多个VLAN隔离广播域，并将这些Sub-VLAN聚合成一个逻辑的VLAN（super-VLAN）
- Sub-VLAN
	- **只包含物理接口**，不能建立VLANIF接口
	- Sub-VLAN的主机与外部通信靠Super-VLAN的三层VLANIF接口
- Super-VLAN
	- **只建立三层VLANIF接口**，不包含物理接口（相当于网关）
	- 为保证sub-VLAN间能通信，**需要开启ARP代理功能**
	- 配置Super-VLAN：先进入VLAN视图
		- 指定当前VLAN为Super-VLAN `aggregate-vlan`
		- 指定成员Sub-VLAN `access-vlan [VLAN_ID1 to VLAN_ID2]`
		- 创建VLANIF `interface vlanif VLANIF_ID`
		- 指定VLANIF的IP配置 `ip address IP地址 子网掩码`
			- 需要注意VLANIF的IP地址要与Sub-VLAN的主机IP在同一网段
		- 开启ARP代理 `arp-proxy inter-sub-vlan-proxy enable`
![[Pasted image 20260418203749.png]]
![[Pasted image 20260418203926.png]]
![[Pasted image 20260418203938.png]]
![[Pasted image 20260418204038.png]]![[Pasted image 20260418204520.png]]![[Pasted image 20260418204527.png]]

## MUX VLAN

![[Pasted image 20260418204912.png]]
- **Separate VLAN内的主机之间也不能通信**
- 配置流程
	- 进入Principal VLAN的视图
	- 启用mux-vlan并将当前vlan设置为Principal `mux-vlan`
	- 设置Group VLAN `subordinate group VLAN_ID`
	- 设置 Separate VLAN `subordinate separate VLAN_ID`
	- 配置所有端口，使端口加入对应VLAN，并启用MUX VLAN功能```
		  interface 端口号
		  port link-type access
		  port default vlan VLAN_ID
		  port mux-vlan enable vlan VLAN_ID
		  ```

![[Pasted image 20260418204956.png]]
![[Pasted image 20260418205100.png]]
![[Pasted image 20260418205202.png]]

## QinQ配置（案例题）

QinQ（802.1Q-in-802.1Q），也叫做VLAN Stacking或Double VLAN，由IEEE 802.1ad标准定义，是一项扩展VLAN空间的技术，**通过在802.1Q标签报文的基础上再增加一层802.1Q的Tag来达到扩展VLAN空间的目的**。一般应用在骨干网中，通过**将用户私网VLAN Tag封装在公网VLAN Tag中**，使报文带着两层VLAN Tag穿越运营商的骨干网络（公网），扩充VLAN数量，实现对用户的精细化管理。

### 基本QinQ

- 为所有内层vlan都加上相同的外层vlan tag
- 配置接口为dot1q-tunnel `port link-type dot1q-tunnel`
![[Pasted image 20260418210832.png]]

### 灵活QinQ

可以针对不同的内层vlan配置不同的外层vlan tag
配置流程
- 创建VLAN
- 进入端口视图
- 设置端口类型为 hybrid `port link-type hybrid`
- 连接外网的端口设置为 untagged `port hybrid untagged vlan 外部VLAN_ID`
- 开启QinQ `qinq vlan-translation enable`
- 将内部VLAN包装为外部VLAN `port vlan-stacking vlan 内部VLAN_ID stack-vlan 外部VLAN_ID`

![[Pasted image 20260418210659.png]]

### VLAN Mapping配置

- 将QinQ映射为单个VLAN `port vlan-mapping vlan 外层VLAN_ID inner-vlan 内层VLAN_ID map-single-vlan 映射VLAN_ID`
- **操作完后需要commit**

![[Pasted image 20260418211954.png]]
![[Pasted image 20260418212031.png]]
![[Pasted image 20260418211938.png]]
![[Pasted image 20260418211859.png]]

## VLAN间通信（案例题）

### 路由器（单臂路由）

子接口：以物理接口ID+子接口ID进行标识的逻辑接口，子接口可以终结带VLAN tag的数据帧
配置流程
- 进入逻辑子接口 `interface 物理接口ID.子接口ID`
- 配置终结VLAN tag `dot1q termination vid VLAN_ID`
	- 接收报文时，去掉该tag
	- 发送报文时，加上该tag
- 配置接口的IP
- 开启ARP广播代理 `arp broadcast enable`
	- 路由器会屏蔽ARP

![[Pasted image 20260418213217.png]]
![[Pasted image 20260418212914.png]]

### 三层交换机

配置步骤
- 配置VLANIF
- 将PC的默认网关设置为对应的VLANIF

![[Pasted image 20260418213819.png]]![[Pasted image 20260418213834.png]]