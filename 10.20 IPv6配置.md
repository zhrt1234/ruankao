- 填空题：地址计算
- 隧道技术
- NAT64、DNS64技术
## NAT64和DNS64

- DNS64功能：将IPv4域名转换为IPv6地址
- NAT64功能
	- 收到IPv6包后，提取里面的IPv4地址，把IPv6报头改成IPv4报头，发给IPv4网段
	- 收到IPv4包后，把IPv4报头改成IPv6报头，发给IPv6网段

### NAT64

![[Pasted image 20260427215236.png]]
- **第64~71位固定填0**
- **32、64、96位前缀长度：不用拆分IPv4地址**

#### NAT64配置

```shell
interface 端口号
	nat64 enable # 在端口启用nat64
nat address-group 地址池名 # 创建IPv4地址池
	mode pat # 使用PAT模式
	section 1 起始IP地址 终止IP地址 # 设置地址池中的IPv4地址
nat-policy
	rule name NAT规则名 # 创建NAT规则
		nat-type nat64 # 设置NAT类型为NAT64
		source-zone 区域名 # 设置源区域（IPv6网段）
		destination-zone 区域名 # 设置目的区域（IPv4网段）
		source-address IPv6地址 前缀长度 # 设置允许通过的IPv6地址
		action source-nat address-group 地址池名 # 将通过的IPv6地址转换为地址池中的IPv4地址
```
![[Pasted image 20260427220054.png]]
![[Pasted image 20260427220035.png]]

## 隧道技术

### IPv6 Over IPv4 GRE隧道

- 通过GRE隧道技术，使IPv6报文在IPv4网络中传输
- IPv6 Over IPv4 GRE隧道是**手动隧道**

```shell
interface tunnel 隧道编号 # 创建tunnel接口
	tunnel-protocol gre  # 配置tunnel接口的隧道协议为GRE
	source {IP地址 | 端口号} # 配置tunnel的源地址或源端口
	destination IP地址   # 配置tunnel的目的地址
	ipv6 enable          # 启用接口的IPv6功能
```

![[Pasted image 20260427220538.png]]

### ISATAP隧道

![[Pasted image 20260427224339.png]]
- ISATAP隧道是**自动隧道**
- 地址封装前64位：`0000:5EFE:IPv4地址::/64`

ISATAP路由器配置
```shell
interface tunnel 隧道编号 # 创建tunnel接口
	tunnel-protocol ipv6-ipv4 isatap # 配置tunnel接口的隧道协议为IPv6-IPv4，并使用ISATAP隧道
	ipv6 enable # 开启接口IPv6功能
	ipv6 address 2001::/64 eui-64 # 配置接口的EUI-64格式的全球单播地址
	ipv6 address IPv6地址 前缀长度 # 配置接口IPv6地址
	source {IP地址 | 端口号} # 配置tunnel的源地址或源端口
	
	
```
ISATAP主机配置
```shell
netsh interface ipv6 isatap set router 路由器IP # 为ISATAP隧道添加到路由器的静态路由
```
 ![[Pasted image 20260427221245.png]]![[Pasted image 20260427221216.png]]
![[Pasted image 20260427221026.png]]

### 6 to 4隧道

- 6 to 4隧道是自动隧道
- 地址封装前48位：`2002:IPv4地址::/48`
- 传输流程
	- 将IPv4地址封装为IPv6地址，发送IPv6数据包
	- 在路由器A中，将IPv6数据包通过IPv4报头包装，在互联网中传输
	- 路由器B收到后，去掉IPv4报头，得到IPv6数据包
![[Pasted image 20260427221740.png]]
![[Pasted image 20260427225930.png]]


## 案例题

![[Pasted image 20260427222311.png]]
![[Pasted image 20260427222923.png]]