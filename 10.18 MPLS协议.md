
- 协议原理
- 配置步骤、关键代码


## 基本概念

- 传统IP路由转发特点
	- 所有路由器都需要知道全网的路由
	- IP头部不定长、处理效率低
	- 传统IP转发是面向无连接的，无法提供端到端的QoS保证

![[Pasted image 20260427152656.png]]
**填空题：MPLS域、LER、LSR、核心LSR**
- **MPLS域**：一系列连续的运行MPLS的网络设备
- **LSR标签交换路由器**：支持MPLS的路由器
- 基于LSR的位置分类
	- **LER边沿路由器**：连接其他网络的LSR
	- **核心LSR**：区域内部的LSR
- 基于数据处理方式分类
	- **入站LSR（Ingress LSR）：标签push操作**，向IP报文中压入MPLS头部
	- **中转LSR（Transit LSR）：标签swap操作**，将MPLS报文进行标签置换，并继续转发
	- **出站LSR（Egress LSR）：标签pop操作**，将MPLS报文中MPLS头部移除，还原为IP报文

## MPLS标签

- MPLS支持多层标签头部，多层MPLS标签主要用于MPLS VPN
- 标签栈（Label Stack）：标签头部的集合
	- 最靠近以太帧头部的是栈顶标签
	- 最靠近IP头部的是栈底标签
![[Pasted image 20260427153930.png]]

### LSP标签交换路径

- **LSP标签交换路径**：IP报文在MPLS网络中经过的路径
- 静态LSP
	- 用户通过手工为各个FEC分配标签
	- 适用于拓扑结构简单、稳定的小型网络
	- **标签分配原则（代码题）**：前一个节点 **Out标签的值** 等于下一个节点的 **In标签的值**
- 动态LSP
	- 使用**标签发布协议LDP**

### 静态LSP配置

- 在路由器上启用MPLS（全局和端口都要启用）
```shell
mpls lsr-id ISR_ID # 设置设备的LSR_ID，一般为IP地址
mpls # 在全局启用mpls
	quit
interface 端口号
	mpls # 在端口启用mpls
	quit
```
- 配置入站LSR `static-lsp ingress 静态LSP名 destination 目的IP 子网掩码 {nexthop 下一跳IP | outgoing-interface 端口号} out-label 出标签`
	- `destination 目的IP 子网掩码` 设置路由的终点
	- `outgoing-interface 端口号` 设置出口的端口
- 配置中转LSR `static-lsp transit 静态LSP名 incoming-interface 端口号 in-label 入标签 {nexthop 下一跳IP | outgoing-interface 端口号} out-label 出标签`
	- `incoming-interface 端口号` 设置入口的端口
- 配置出站LSR `static-lsp egress 静态LSP名 incoming-interface 端口号 in-label 入标签`
#### 静态LSP配置案例

![[Pasted image 20260426230642.png]]
![[Pasted image 20260426231027.png]]

### 动态LSP配置

#### LDP标签分发协议

![[Pasted image 20260427162237.png]]
- 路由器把自己的**In标签**发送给其他路由器，告知它们应该打怎样的**Out标签**
- 运行MPLS的路由器中必须同时运行普通路由协议
- LDP用于VPN服务上，具有组网配置简单、支持大容量LSP等优点

#### 动态LSP配置

在路由器上启用MPLS和LDP（全局和端口都要启用）
```shell
mpls # 启用MPLS
	quit
mpls ldp
	quit # 启用LDP协议
interface 端口号
	mpls     # 启用端口MPLS
	mpls ldp # 启用端口LDP
```
#### 动态LSP配置案例

![[Pasted image 20260426231647.png]]
![[Pasted image 20260426231709.png]]



![[Pasted image 20260426235331.png]]
1）标签
2）LSR标签交换路由器
3）LER边沿标签交换路由
4）LSP标签交换路径
5）入节点
6）出节点
![[c399b6c330e7ee5a80509ae36b371cea.png]]
![[Pasted image 20260426235641.png]]
7）**lsr-id**
8）mpls
9）vlanif
![[Pasted image 20260426235709.png]]
10）out-label
11）20
12）40
13）in-label
![[Pasted image 20260426235823.png]]14）30
15）60