
## IPv4

### IPv4地址
 
IP地址分类
- **A类地址：1~126（0）**
	- 私有地址：**10.x.x.x**
- **B类地址：128~191（10）**
	- 私有地址：**172.16.0.0~172.31.255.255**
- **C类地址：192~223（110）**
	- 私有地址：**192.168.x.x**
- **D类组播地址：224~239（1110）**
- E类保留地址：240~255（11110）

IP地址类型
- 单播地址
- 广播地址
	- 直接广播地址：主机号全1，向指定网络所有节点发送数据包
	- 本地广播地址：255.255.255.255，向本地网络所有节点发送数据包
- 组播地址
	- 组播IP地址：224~239
	- 组播MAC地址：01-00-5E开头
	- 路由协议：224.0.0.0~224.0.0.255
	- 本地管理组播：239.0.0.0~239.255.255.255（本地管理组播地址属于私有地址，在不同的管理域内使用相同的本地管理组播地址不会导致冲突）
	- 网段中所有支持组播的主机：224.0.0.1
	- **OSPF路由器：224.0.0.5**
	- **OSPF指派路由器：224.0.0.6**
	- **RIPv2路由器：224.0.0.9**

特殊IP地址
- 0.0.0.0：本网络上的本主机，可以作为源地址，不可以作为目的地址
- **255.255.255.255**：在本网络上进行广播，不可以作为源地址，可以作为目的地址
- 127.x.x.x：本地环回测试（不能全0、不能全1）
- **169.254.x.x**：临时地址，DHCP故障时操作系统自动分配（不能全0、不能全1）

### IPv4报文

![[Pasted image 20260331164658.png]]
- Header Length（首部长度）：`4bit`，单位为 `4Byte`，最小值为5，IP首部最小长度为 `20Byte`
- Type of Service（服务类型）：QoS服务启用时起作用（IPv6也有）
- Total Length（总长度）：`16bit`，IP报文总长度，最长为 `65535Byte`
- Identification（标识符）：对IP分片做标识，同一报文的分片标识符相同
- Flags（标志）
	- DF：允许分片`DF = 0`
	- MF：是否还有分片，还有分片 `MF = 1`
- Fragment Offset（片偏移）：分片在原报文中的相对位置
	- `13bit`，`8Byte` 为单位，`13bit + 3bit`刚好表示总长度
- TTL（生存时间）：每经过一个路由器TTL减1，当TTL=0时路由器丢弃该报文，并向源IP地址发送ICMP报文
- Padding（填充）：确保IP首部长度为 `4Byte` 的整数倍

## ARP协议

- ARP地址解析协议：IP地址转换为MAC地址
- RARP逆向地址解析协议：MAC地址转换为IP地址
- **工作在网络层，直接封装成帧**
- **通过广播发送ARP报文请求**
- **不同局域网之间通信，IP不变，MAC变**

### ARP报文

![[Pasted image 20260331170506.png|519]]
ARP报文
![[Pasted image 20260331170634.png]]
ARP请求报文
![[Pasted image 20260331171012.png]]
ARP响应报文

- ARP请求报文
	- 发送方MAC地址、发送方IP地址
	- 目的MAC地址（全0）、目的IP地址
- ARP响应报文
	- 发送方MAC地址、发送方IP地址
	- 目的MAC地址、目的IP地址

### ARP指令

- `arp -a`：查看ARP缓存表
- `arp -d`：清除ARP缓存
- `arp -s`：静态绑定ARP条目

### ARP攻击（案例题）
- 免费ARP：设备主动使用自己的IP地址作为目的IP地址发送ARP请求
- ARP洪泛攻击（DoS）：攻击者向设备发送大量伪造ARP请求报文或免费报文
	- ARP表项限制
	- ARP速率抑制
	- ARP表项严格学习
- ARP欺骗攻击：攻击者发送伪造的ARP报文，造成网络中报文转发异常
	- 配置ARP表项固化
	- 配置发送ARP免费报文
- 通用防范方法
	- **主机安装ARP防火墙**
	- **静态绑定IP地址和MAC地址**

## ICMP协议

### ICMP报文类型
- 差错控制报文
	- 目的不可达 `TYPE=3`，路由器或主机不能交付数据报
		- 网络不可达 `code=0`
		- 主机不可达 `code=1`
		- 协议不可达 `code=2`
		- 端口不可达 `code=3`
	- 源点抑制 `TYPE=4`，网络出现拥塞
	- 重定向 `TYPE=5`，路由器发现有更加路径，请求主机改变路由
	- 超时 `TYPE=11`，TTL归零
	- 参数问题 `TYPE=12`，收到的IP报文校验有问题
- 询问报文
	- ECHO请求 `TYPE=8`
	- ECHO回应 `TYPE=0`
	- 时间戳请求 `TYPE=13`
	- 时间戳回应 `TYPE=14`

### ICMP应用
- `ping`：用于测试网络连通性
	- 利用ECHO请求和ECHO响应报文
	- 如果显示 `request time out` 则IP报文丢失
- 路由跟踪命令：利用超时和目的不可达报文
	- `tracert(windows)`
	- `traceroute(linux)`

## QoS服务质量

- 服务质量优先级：管理业务>语音业务>视频业务>上网业务
- QoS度量标准：带宽、时延、丢包率、抖动
- RSVP资源预留协议
	- 接收方向路由器申请资源
	- 不适合因特网

## IPv6

- 地址空间 `128bit`
- 简化的报文头格式 `40Byte`
- 取消三层校验：IP层不再进行校验
- 取消中间节点分片功能：中间路由器不再分片，只在源节点分片，降低路由器CPU消耗
- 定义定长IPv6报头：有利于硬件快速处理

### IPv6报文

![[Pasted image 20260401101816.png]]
IPv6基本报头
- Traffic Class：服务类型、流量类型
- Flow Label：流标签，用于QoS
- Payload Length：除报头外字节长度
- Next Header：下一个扩展报头的位置
- Hop Limit：跳数限制，等于TTL
![[Pasted image 20260401102053.png]]
IPv6扩展报头
- **逐条选项报头：必须放在第一个**，传送路径上每个中间节点都要读取并处理该字段
- 目的选项报头
- 路由报头
- 分段报头
- 认证报头
- 封装安全静载报头

### IPv6地址

IPv6地址表示
- 冒分十六进制法：每4位16进制为一组（16bit）
- 前导零省略
- 连续0压缩：将一组连续的0压缩用 `::` 代替
- 斜线表示法： `\n` 代表其前缀

**IPv6地址分配方式**
- 手动分配
- 自动分配
	- 无状态地址自动分配
	- 有状态地址自动分配
 
IPv6地址类型
- **全球单播地址**
	- **前缀标识：前3位固定001**
- **站点本地地址：在路由器内互通，不能访问Internet**
	- **前缀标识：FEC0::/10**
	- 1111 1110 11
- **链路本地地址：在交换机内互通，不能访问路由器**
	- **前缀标识：FE80::/10**
	- 1111 1110 10
- **多播地址（组播）**
	- **前缀标识：FF00::/8**
	- 1111 1111
- 任播地址：一组接口，只交付给路由距离最近的一个接口，仅分配给路由器

特殊地址
- **默认地址**：`0:0:0:0:0:0:0:0/128 或 ::`，相当于 `0.0.0.0`
- **环回地址**：`0:0:0:0:0:0:0:1/128`，相当于 `127.0.0.1`
- IPv4映射地址：`0:0:0:0:0:FFFF:w.x.y.z` 或 `::FFFF:w.x.y.z`
- 6to4地址：用于在IPv4网络上传输IPv6报文：`2002:WWXX:YYZZ:`
- 6over4地址：用于6over4隧道技术的地址： `[64bit前缀]:0:0:WWXX:YYZZ`
- ISATAP地址：`::0:5EFE:w.x.y.z`

### IPv6协议

- **ICMPv6**：**用于路径MTU发现**、地址解析、NDP协议、重复地址检测
- **NDP邻居发现协议**（相当于ARP/RARP协议）
	- 邻居请求消息NS
	- 邻居通告消息NA
	- 路由请求消息RS
	- 路由通告消息RA

### IPv6过渡技术
- **双协议栈技术**
	- 使用支持双协议栈的主机、路由器
- **隧道技术**
	- 把IPv6报文封装在IPv4报文的数据部分中
	- 使用IPv4网络通信
	- **ISATAP隧道**：`::0000:5EFE:w.x.y.z`
	- **IPv4兼容IPv6自动隧道**：`0:0:0:0:0:0:w.x.y.z` 或 `::w.x.y.z`
	- **6to4隧道**：`2002:w.x.y.z::/48`，48位前缀
![[Pasted image 20260401111453.png]]
- **网络地址转换技术**（NAT64 + DNS64）
	- DNS64将DNS查询信息中的A记录（IPv4地址）转换为AAAA记录（IPv6地址）
	- **NAT64在地址转换时，需要将第64~71位设置为0**![[Pasted image 20260401111923.png]]

## UDP
![Alt text](1774284436447.png)

- 首部开销`8Byte`
- 整个报文最大长度`65535Byte`，数据部分最大长度`(65535 - 8)Byte`


## TCP

### TCP报文格式
![Alt text](1774267876150.png)

- 首部开销`20Byte`
- 源端口、目的端口：`0~65535`
- 序列号seq：`0~2^32-1`，本报文数据的第一个字节的index
- 确认号ack：`0~2^32-1`，期望收到的下一个报文的序列号
- 数据偏移offset：`4bit`，TCP首部长度，以4Byte为单位，默认为`5（即20Byte）`
- 控制字段
	- URG：发送紧急数据，为1时紧急指针有效
	- ACK：表示应答，为1时确认号ack有效
	- RST：TCP连接有严重错误，需要重新建立连接，也可用于拒绝连接
	- SYN：发起TCP连接请求
	- FIN：结束TCP连接
- 窗口字段：`0~65535`，接收窗口大小
- 校验和
- 紧急指针：指向需要紧急发送的数据

### 三次握手
![Alt text](1774267836288.png)


### 四次挥手
![Alt text](1774267821731.png)


### 流量控制
- 滑动窗口：接收方win字段（16bit）
- 重传计时器：超时重传，`新RTTs = (1 - α) x 旧RTTs + α x 新RTT样本`
- 坚持计时器：防止滑动窗口为0的死锁，超时则强行发送数据
- 保活计时器：防止TCP链接长时间空闲

### 拥塞控制
![Alt text](1774267117796.png)

- cwnd拥塞窗口
- ssthresh慢开始门限
- 慢开始：`cwnd < ssthresh`，初始`cwnd = 1`，每经过一轮`cwnd * 2`
- 拥塞避免：`cwnd >= ssthresh`，每经过一轮 `cwnd + 1`
- **乘法减小：出现超时（即可能存在网络拥塞）**，`ssthresh = cwnd / 2，cwnd = 1`
- 快重传：接收方收到失序报文段，发送重复确认ACK
- **快恢复：发送发收到3个重复确认ACK**，`ssthresh = cwnd = cwnd / 2`

## 端口号

- 熟知端口：`0~1023`
- TCP端口
	- FTP协议：20数据传输，21控制
	- SSH协议：22
	- Telnet：23
	- SMTP简单邮件发送协议：25
	- POP3邮箱接收服务器：110
	- IMAP3因特网信息访问协议：143
	- BGP路由协议：179
	- HTTPS：443
- UDP端口
	- DNS：53
	- DHCP：67服务器，68客户端
	- TFTP简单文件传输协议：69
	- SNMP简单网络管理协议：
		- 161管理站查询，客户端响应
		- 162发送trap信息，向管理站通知设备状态变化
	- RIP路由信息协议：520

## QUIC协议

- 快速建立连接
- 无队头阻塞的多路复用
- 无歧义重创：重传时seq递增
- 支持连接迁移：端口或IP变化时保持连接
- 向前纠错（FEC）：类似于Raid5进行冗余和纠错

## DNS协议
![Alt text](1774352883790.png)
 
- **UDP协议的53端口**
- DNS服务器与客户端之间使用UDP，DNS服务器之间使用TCP

![Alt text](1774353094713.png)

- 本地域名服务器：主域名服务器
- 辅助域名服务器：主域名服务器的备份，通过TCP的53端口保持连接
- 转发域名服务器：负责非本地区域的域名解析
- 缓存域名服务器：只用于缓存的DNS服务器，没有自己的区域数据库文件

![Alt text](1774353669790.png)

DNS查询方式
- 迭代：告知请求者下一步应该找谁
- 递归：代替请求者进行查询并返回最终结果

DNS资源记录
- A（主机）：域名->IPv4地址
- AAAA（主机）：域名->IPv6地址
- SOA（起始授权机构）：定义了权威域名服务器
- NS（域名服务器）：主域名服务器和辅助域名服务器
- PTR（指针）：IP地址->域名
- CNAME（别名记录）：基于A记录的主机作出别名
- MX（邮件交换记录）：指向邮件服务器地址

![Alt text](1774354195610.png)
A向B的邮件服务器发邮件，因此要解析B的邮件服务器域名

## DHCP

- **DHCP协议是BOOTP协议的扩栈**
![Alt text](1774447570423.png)
**首次接入网络：DORA**，客户端在收到ack报文前始终使用0.0.0.0地址
1. 客户端**DHCP Discover**：发现DHCP服务器
2. 服务器**DHCP Offer**：分配IP地址
3. 客户端**DHCP Request**：告知服务器自己将使用该IP地址
4. 服务器**DHCP Ack**：告知客户端可以使用该IP地址

![Alt text](1774447700720.png)
**DHCP续约**
**DHCP分配过程中的异常**：
- 重发4次DHCP请求未获得IP地址，自动分配**169.254.0.0/16**网段的一个地址
- 当客户端发现服务器分配的IP地址无法使用，将发出**DHCP Decline**报文，通知服务器禁止使用该IP地址

![Alt text](1774448276355.png)
在DHCP服务域内可以使用静态IP地址

![Alt text](1774448813139.png)
DHCP中继：为不同网段的主机分配IP地址

![Alt text](1774448450726.png)
DHCP服务器可以为客户端分配
- IP地址、子网掩码、默认网关
- 首选DNS服务器、备用DNS服务器

![Alt text](1774448765999.png)
错题

ipconfig指令：
- `ipconfig /all`：显示TCP/IP配置的详细信息
- `ipconfig /release`：释放IP地址
- `ipconfig /renew`：重新向DHCP服务器申请IP地址

![Alt text](1774449316376.png)
**DHCP snooping**：抵御针对DHCP的攻击
- 为交换机配置信任端口，只接收该端口的DHCP offer报文
- DHCP snooping绑定表

**DHCP攻击（案例题）**
- **DHCP Server仿冒者攻击**
- **DHCP Server拒绝服务攻击**
- **仿冒DHCP报文攻击**

## HTTP协议
- TCP协议，默认端口80
- URL：`http://<主机>[:端口]/<路径>/文件名`
- HTTP访问过程
	- 浏览器分析URL
	- 浏览器向DNS请求解析URL中域名对应的IP地址
	- 浏览器与服务器建立TCP连接
	- 浏览器发出读取文件指令GET
	- 服务器发回响应，将文件发送给浏览器
	- TCP连接释放
	- 浏览器显示网页内容

![Alt text](1774679795662.png)
GET与POST的区别

HTTP版本
- 1.0：**每次收发任何数据都需重新建立TCP连接**
- 1.1：**持久连接**、管道机制
	- 非流水线方式：每收到一个相应后才发出下一个请求
	- 流水线方式：客户端可以在收到前一个响应之前，连续发送多个HTTP请求
- 2.0：二进制协议、多工、头部信息压缩、服务器推送
- 3.0：基于QUIC

![Alt text](1774679903360.png)
HTTP1.0每次请求一个文档都需要建立TCP连接

![Alt text](1774680321481.png)
HTTP1.1非流水线方式

## FTP协议

- 控制端口**21**
- 数据端口
	- 主动模式：**20**
	- 被动模式：服务器指定随机端口（**≥1024**）
- 匿名访问用户名：anonymous
- GET下载，PUT上传

![Alt text](1774679671427.png)
主动模式

![Alt text](1774679708513.png)
被动模式

## 电子邮件协议
- 电子邮件地址：`user@server.com`
	- user：邮件用户名
	- server.com：邮件服务器域名

![Alt text](1774680911264.png)
邮件协议
- SMTP最初设计时仅支持**ASCII字符集**，这意味着它只能传输基本的英文字符。然而，随着传输多媒体内容（如图片、音频等）变得越来越重要。为了解决这个问题，SMTP引入了**MIME协议**

![Alt text](1774680830795.png)
邮件发送过程

## Telnet协议
- **基于TCP协议，端口号23**
- 使用**NVT格式**传输字符
- 使用指令：`telnet IP地址`
![Alt text](1774681473581.png)

![Alt text](1774681489631.png)
telnet与ssh区别

![Alt text](1774681581421.png)
本地计算机应设置出站规则

## 互联网新技术

### 大数据

### 云计算
- 云计算是分布式计算的一种
- 云计算模式：
	- SaaS软件即服务
	- PaaS平台即服务
	- IaaS基础设施即服务
- 公有云、私有云、混合云

![Alt text](1774707050206.png)

### 物联网
- 物联网（**IoT**）特点：任何时间、任何地点，人、机、物互联互通
- 短距离无线通信技术：
	- 蓝牙（IEEE 802.15.1）
	- WIFI
	- **Zigbee（IEEE 802.15.4）：智能家居，自组织、自愈合功能**
	- RFID
	- 星闪nearlink
- 广域无线通信技术
	- NB-lot
		- **广覆盖、深度覆盖**
		- **海量连接**
		- **低功耗、低成本、低带宽**
		- **可以与现有网络基站复用**
		- 部署频率是授权的，必须由运营商部署
		- 适合共享单车

### 区块链
- **区块链是去中心化的分布式账本数据库**
- 特点：去中心化、匿名性、透明性、不可篡改
- 记账算法
	- PoW：比谁的算力大，能源消耗大，比特币
	- PoS：权益证明，比谁的代币数量多，以太坊
- 区块链分叉问题和双花问题
![Alt text](1774708065561.png)
- 区块链类型：公有区块链、私有区块链、联盟区块链
- 区块链优点
	- 去中心化
	- 数据信息透明，便于追踪
	- 分布式记账容错性高
	- 匿名性保护隐私
- 区块链缺陷
	- 高耗能问题
	- 存储空间利用率问题
	- 处理大规模交易的抗压能力问题
	- 安全性问题

### 软件定义网络SDN
- SDN是**将网络设备的控制平面与数据平面分离**，从而实现网络流量控制的技术
- SDN包括控制层、转发层、应用层
- SDN特征：转控分离、集中控制、开放可编程接口

![Alt text](1774708662177.png)

### 人工智能
- 人工智能四要素：数据、算法、算力、场景