
**考点**
- 动态路由基本功能
- 常用路由协议、协议间的区别、协议使用场景
- RIP、OSPF基本工作原理、路由更新机制、算法特点、配置代码
- ISIS基本概念
- BGP基本概念、路径转发机制、算法特点、配置代码
- BFD与RIP、OSPF的快速联动


## 路由协议

- 路由协议：用来计算、维护路由信息的协议
- 动态路由协议的工作过程的几个阶段
	- 邻居发现阶段
	- 交换路由信息阶段
	- 计算路由阶段
	- 维护路由阶段
- **AS自治系统**：通过编号区分不同的AS
- **IGP内部网关路由协议**：在同一个AS内运行的路由协议，RIP、OSPF、IS-IS
- **EGP外部网关路由协议**：在不同AS间运行的路由协议，BGP
![[Pasted image 20260419214236.png]]

### 路由协议分类

- **距离矢量路由协议**：以路由器作为一跳
	- **RIP**、IGRP
	- 周期性更新路由信息、适合小型网络
- **链路状态路由协议**
	- **OSPF**、**IS-IS**
	- 触发更新、适合大型网络
- **路径矢量路由协议**：以AS作为一跳
	- **BGP**
	- 不同AS之间路由
- 混合路由协议
	- EIGRP
![[Pasted image 20260419214556.png]]

![[Pasted image 20260419214933.png]]
路由协议是在路由器之间传送信息

## RIP路由协议

- **以30秒为周期向邻居发送整个路由表**
- 超时时间为180秒
- 删除路由信息是120秒
- **以跳数作为唯一度量值，跳数越小优先，最大跳数为15跳，16跳为不可达**
- 收敛：经过一系列路由更新，网络中每个路由器都具有一张完整的路由表的过程
- 端口号：**UDP 520端口**
- **RIP的特点**
	- 实现原理简单
	- 16跳即为不可达，只适合小规模网络
	- 收敛时间慢，容易产生路由环路（坏消息传的慢）

![[Pasted image 20260419215642.png]]
**路由条目更新规则**
- 发现新网络，更新
- 相同下一跳，最新消息，更新
- 不同下一跳
	- 新路由优先级高，更新
	- 新路由优先级低，不更新
	- 优先级相同，等价负载均衡
### RIP环路问题

![[Pasted image 20260419220434.png|440]]
当某个IP不可达时，这一消息传播的很慢，**需要等所有路由表中的跳数超过15**

![[Pasted image 20260419220617.png]]

### RIP版本

RIPv1
- **广播发送RIP报文**
- **仅支持有类路由**，如A、B、C类网络

RIPv2
- **广播和组播发送RIP报文**
	- 组播地址：**224.0.0.9**
	- 当运行广播模式时，可以接受RIPv1的报文
- **支持无类域间路由、可变长子网掩码（VLMS）**、路由汇聚
- **支持明文认证和MD5密文认证**

![[Pasted image 20260419221200.png]]

### RIP配置

![[Pasted image 20260419234746.png]]

![[Pasted image 20260419234952.png]]
![[Pasted image 20260419234938.png]]

![[Pasted image 20260419235131.png]]
错题，需要在RIP视图下进行配置

### RIP与BFD联动

BFD双向转发检测：提供链路通信故障的**快速检测机制**，并及时通知上层应用。
- 作用：**实线路由快速收敛，链路故障感知**
- 动态BFD：通过上层应用（RIP/OSPF）的邻居发现机制
- 静态BFD：通过静态配置手动添加对端邻居信息来创建回话

#### 配置RIP与动态BFD

- 全局启用BFD，并进入BFD视图 `bfd`
- RIP进程下启用BFD：网络中大部分RIP接口需要启动BFD
	- 所有接口启用BFD功能 `bfd all-interfaces enable`
	- 配置BFD最小发送、接受bfd报文间隔和检测倍数 `bfd all-interfaces min-tx-interval 最小发送间隔 min-rx-interval 最小接受间隔 detect-multiplier 检测倍数`
- RIP接口下启用BFD：网络中只有小部分RIP接口需要启动BFD
	- 端口切换为三层模式 `undo portswitch`
	- 启用接口BFD会话 `rip bfd enable`
	- 阻止该接口创建BFD会话 `rip bfd block`

![[Pasted image 20260420000354.png]]
![[Pasted image 20260420000556.png]]

#### 配置RIP与静态BFD

- 单臂BFD检测：支持BFD的设备与不支持BFD的设备对接，在支持的设备上配置BFD进行单向检测
- 普通BFD检测：两端设备都支持BFD，在两端都配置静态BFD

```shell
bfd BFD编号 bind peer-ip 对端IP interface 本端口 source-ip 本 IP one-arm-echo # 配置接口静态BFD
interface 端口号
	rip bfd static # 启动接口的静态BFD特性
```
- 当使用单臂BFD时，加上参数 `one-arm-echo`

![[Pasted image 20260420002204.png]]

## OSPF协议

- **SPF最短路径最优算法（基于Dijkstra）**，无环
- IPv4使用OSPFv2，IPv6使用OSPFv3
- OSPF使用**组播**
- OSPF直接将**数据封装在IP包中，协议号为89**

### OSPF基本原理

**OSPF协议工作原理（要背）**
1. 周期性发送hello报文，发现**有效邻居**
	- 每**10秒**发送一次hello包，**40秒**没有收到hello包，认为邻居失效
2. 邻接关系的路由器传递链路状态信息 （**LSA链路状态通告**）
   ![[Pasted image 20260420204008.png|302]]
3. 所有OSPF路由器均有一个**LSDB链路状态数据库**，形成带权有向图
   ![[Pasted image 20260420204142.png|468]]
4. 使用**SPF算法**计算出最佳路由![[Pasted image 20260420204337.png]]
	- 默认开销 `COST = 100Mbps / 接口带宽Mbps`
		- 其中100Mbps是默认值，可以通过 `bandwidth-reference 带宽` 配置
	- 每个路由器都计算出一个以自己为起点的最佳路由![[Pasted image 20260420204402.png]]
5. **支持触发更新**
	- 收敛速度快
	- 避免网络资源浪费

#### OSPF的网络类型
- 设置OSPF的网络类型 `ospf network-type {broadcast | p2p | nbma | p2mp}`
	- **broadcast广播**，通过以太网口连接
	- **P2P点对点**
	- NBMA
	- P2MP
	- 链路两端的OSPF接口网络类型必须一致，否则无法建立邻居关系

#### Router ID选举
- 指定router ID `router-id ID`
	- OSPF进程手工配置的router ID优先级最高
	- 全局模式下配置的公用router ID优先级第二
- 选取路由器**loopback接口上数值最高的IP地址**作为Router ID
- 如果没有loopback接口，在**物理端口中选取数值最高的IP地址**作为Router ID

#### DR和BDR(综合题）
![[Pasted image 20260420203040.png]]
MA网络（广播网络）中的问题
- N x (N-1) / 2个邻接关系，管理复杂
- 重复的LSA泛洪，造成资源浪费
- 解决方法：在MA网络中选举DR
	- **DR（Designed Router指定路由器）**：负责在MA网络中建立和维护邻接关系、同步LSA
	- **BDR（备份指定路由器）**：在DR失效时接管DR工作
	- 邻接关系
		- **DR与其他所有路由器形成邻接关系，交换链路状态信息**
		- 其他路由器之间不直接交换链路状态信息
	- **监听组播地址**
		- DR/BDR：`224.0.0.6`
		- 其他路由器：`224.0.0.5`
- **DR选举规则**
	- **DR的作用范围是一个广播域（网段）**
	- **比较优先级（0~255），数值越大优先级越高，优先级为0的不参与选举**
	- **比较router ID，ID越大越优先**
	- **新加入的设备不能直接成为DR、BDR**
	  ![[Pasted image 20260420203702.png|353]]
- **设置OSPF中路由器的DR优先级** `ospf dr-priority 优先级`

#### OSPF状态
- `Down` 邻居会话的初始阶段（稳定）
- `Attempt` 适用于NBMA网络，邻居路由器是手工配置的
- `Init` 已经收到了邻居的hello报文，但对方还没有收到本机的hello报文
- `2-Way` 互为邻居（稳定）
- `Exstart` 协商主从关系
- `Exchange` 交换DD报文
- `Loading` 正在同步
- `Full` 建立邻接关系（稳定）

#### OSPF分组类型（了解）

- 问候hello分组
- 数据库描述分组
- 链路请求LSR分组
- 链路状态更新LSU分组
- 链路状态确认LSAck分组

![[Pasted image 20260421005514.png]]
R4为DR，R1和R2不能进入邻接关系，所以最多进行到邻居关系2-Way
![[Pasted image 20260421005846.png]]
DR协议用于广播网络，因此作用范围是一个网段（广播域）

![[Pasted image 20260421212913.png|931]]
- OSPF设置DR优先级为0：`ospf dr-priority 0`
- 将R4的loopback接口的优先级设置的高于R5的loopback接口的优先级
- 将两个路由器的loopback接口的优先级设置相同，R4的loopback接口地址设置为高于R5的loopback接口地址

### OSPF分区管理

- OSPF支持将一个大的AS（自治系统）划分为几个小的区域（Area）
	- **区域0为骨干区域**
	- **非骨干区域必须与骨干区域相连**
- 特点
	- 区域内部路由器拥有相同的LSDB
	- 能降低对路由器内存和CPU资源的消耗
- 路由器分类
	- **ABR区域边界路由器**：在两个区域交界
	- **ASBR自治系统边界路由器**：与另一个AS的路由器相连
	![[Pasted image 20260421213758.png|496]]

#### OSPF中的LSA
- 1类LSA（路由器LSA）
	- 路由器的直连拓扑信息
	- 仅在区域内传播
- 2类LSA（网络LSA）
	- 由广播网络中的DR生成，描述DR与其他路由器的邻接关系
	- 仅在区域内传播
- 3类LSA（网络汇总LSA）
	- 由ABR生成，将一个区域的路由信息汇总并通告给其他区域
- 4类LSA（ASBR汇总LSA）
	- 由ABR生成，描述到ASBR的路由信息，为5类LSA提供支持
- 5类LSA（AS外部LSA）
	- 由ASBR生成，通告OSPF域外的路由信息
	- 例如：引入RIP、BGP等外部路由
- 7类LSA（NSSA LSA）
	- 由ASBR生成，通告OSPF域外的路由信息
	- 仅在NSSA非纯末梢区域内传播，必要时由ABR转换为5类LSA

#### OSPF区域类型
- **骨干区域（Area0）**
	- **非骨干区域必须与骨干区域相连**
	- **非骨干区域通过骨干区域交换路由信息**
- **标准区域**
  ![[Pasted image 20260421220015.png]]
	- 可以接受任何链路更新信息和路由汇总信息（1~5类LSA）
- **末梢区域**（Stub区域）![[Pasted image 20260421220225.png]]
	- 学习区域内路由、区域间路由（1~3类LSA）
	- 不学习其他AS的路由
	- 对AS以外的目标采用默认路由0.0.0.0
- **完全末梢区域**（Totally Stub区域）![[Pasted image 20260421220320.png]]
	- 只学习区域内路由（1~2类LSA）
	- 不学习域间路由、其他AS路由
- **非纯末梢区域**（NSSA）![[Pasted image 20260421220434.png]]
	- 类似于末梢区域，但允许接受NSSA外部LSA（其他AS的路由）

### OSPF配置命令

- `ospf [进程号] [router-id ROUTER_ID]` 启动OSPF进程
	- 不同的进程代表不同的协议
	- 两台使用OSPF的设备，即使进程号不同，也可以建立邻接关系 
- `area 区域ID` 创建并进入OSPF区域视图
- `network 网络地址 反子网掩码` 在区域视图中指定与该路由器直连的网络地址，注意采用**反子网掩码**
- `display ospf peer brief` 查看OSPF邻居表
  ![[Pasted image 20260506230439.png]]
- `display ospf lsdb` 查看OSPF的链路状态数据库![[Pasted image 20260506230429.png]]
在接口视图下
- `ospf enable [进程号] area 区域ID` 启用接口的OSPF功能 
- `ospf cost cost` 手动设置接口cost值

![[Pasted image 20260421222740.png]]![[Pasted image 20260421222955.png]]
![[Pasted image 20260421223131.png]]

#### OSPF区域认证
- 区域认证：在OSPF区域视图下配置
	- 配置简单认证 `authentication-mode simple {plain | cipher} 密码`
	- 配置MD5认证 `authentication-mode {md5 | hmac-md5} KEY_ID {plain | cipher} 密码`
		- `KEY_ID` 如果设备上有多个密钥，用来表示特定的密钥
		- `plain | cipher` 使用明文或密文传输
		- `password` 用于认证的密码
	- 配置SHA256认证 `authentication-mode hmac-sha256 KEY_ID 密码`
- 接口认证：配置OSPF接口的验证模式，在区域认证的指令前面加上 `ospf`
	- `ospf authentication-mode {simple hmac-md5 | hmac-sha256}`
![[Pasted image 20260421230543.png]]

#### OSPF抑制接口（静默端口）
在OSPF视图下，抑制接口接收和发送OSPF报文 `silent-interface {all | 端口号}`
![[Pasted image 20260421230753.png]]

#### OSPF虚连接（案例题）
- 适用场景：**骨干区域与非骨干区域没有直接相连**
- 功能：骨干区域与非骨干区域建立逻辑上的连接通道
- 在OSPF的区域视图下 `vlink-peer ROUTER_ID`
![[Pasted image 20260421231213.png]]

#### OSPF路由汇聚
在OSPF的区域视图下执行
- 在ABR汇总区域间路由，得到3类LSA `abr-summary IP地址 子网掩码`
- 在ASBR汇总AS间路由，得到5类LSA `asbr-summary IP地址 子网掩码`
![[Pasted image 20260421231349.png]]
### OSPF与RIP的区别（案例题）

![[Pasted image 20260421223609.png]]
算法原理
- OSPF是链路状态路由协议
- RIP是距离矢量路由协议

适用范围
- OSPF适用于中大型的路由网络
- RIP只支持最大15跳的路由距离，只适用于小型网络

功能特性
- OSPF
	- 使用链路状态作为路径开销，使用基于Dijstra的SPF算法计算最佳路由
	- 支持触发式更新，收敛速度快
	- 支持可变长子网掩码
	- 路由协议优先级为10，可信任度高
- RIP
	- 基于路由跳数计算路由距离
	- 通过邻居路由器周期性发送路由表更新并计算路由，收敛速度慢，容易产生路由环路
	- RIPv1只支持有类网络，RIPv2支持可变长子网掩码
	- 路由协议优先级为100，可信任度不高

![[Pasted image 20260421232416.png]]
1）在RTE和RTC的**Area2视图**下执行vlink-peer
2）将Area1设置为NSSA非纯末梢区域
3）供外来人员使用的接口设置为静默接口
4）E1+E2

![[Pasted image 20260421232623.png]]
OSPF的需求与对应的解决方案

![[Pasted image 20260421234253.png]]
![[Pasted image 20260421234240.png]]
故障原因：Core-SW和Agg-SW接口所属网络类型不一致，Agg-SW的接口是Broadcast网络，而Core-SW的接口是P2P网络。
解决方法：将Core-SW和Agg-SW接口的网络类型都设置为Broadcast网络，在Core-SW的接口视图下执行`ospf network-type broadcast`指令

![[Pasted image 20260421234650.png]]
![[Pasted image 20260421234634.png]]
6）BFD
7）ASBR/5类
8）静默端口
9）区域

## IS-IS协议

- **链路状态路由协议**
- **运用SPF算法**
- 全网拥有相同的LSDB
- 支持骨干区域和非骨干区域量级的分层结构
	- Level-1路由器部署在非骨干区域
	- Level-和Level-1-2部署在骨干区域
- 区域的边界是物理链路，路由器的所有接口都在同一个区域
	- OSPF区域的边界是路由器，不同接口可能在不同区域
- 适用于大规模的路由网络，运营商内部常用
![[Pasted image 20260421235135.png|404]]